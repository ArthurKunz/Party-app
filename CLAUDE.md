@AGENTS.md

# CLAUDE.md

This is a party and event discovery app for people aged 14–25.
Read this entire file before writing any code.

---

## Project summary
A web app where hosts create public or private events and guests
discover and RSVP to them. Two user roles: host and guest.
Current phase: V1 MVP. Deadline: July 2026.

## Problem statement
For students and young people aged 14–25, finding out what's
happening socially still depends on being in the right WhatsApp
group or following the right person on Instagram. If you're new
to a city or university, events are invisible to you and you're
invisible to them. Facebook Events has discovery but is abandoned
by this generation. Hosts have no dedicated tool to manage
attendance or build credibility — it all gets lost across group
chats and stories.

## Tech stack
- Framework: Next.js 14, App Router only
- Language: TypeScript strict mode
- Database: Supabase (Postgres)
- Auth: Supabase Auth via @supabase/ssr
- Styling: Tailwind CSS

## Coding rules
- Always use my global.css variables. Never use things like bg-green-500 or bg-[#fff], but always use the variables. If there are no matching variables, ask me for permisson to create those.
- Icons: ONLY `lucide-react`. Never hand-roll an `<svg>` for an icon — no inline paths for chevrons, arrows, plus, close, etc. If lucide has no matching icon, ask me before drawing one. (The two deliberate exceptions are a brand mark lucide dropped — the Google logo on the auth sheet — and genuine illustration, not iconography.)
- The only other symbols allowed are Apple emojis (the native system emoji, e.g. 👤 🎂 📸 🤫 ⚖️). Emoji for flavour and list bullets, lucide for interface icons — never a drawn substitute for either.
- update the CLAUDE.md file after each change yourself
- Never use `any` type
- Always ask for my permission to commit or add something
- call me 'Arthur' every time you respond
- Always give me a summary of all changes at the end of all responses
- Always use App Router patterns — never Pages Router
- Default to server components, use `use client` only when needed
- Use createServerClient in server components and API routes
- Use createBrowserClient in client components
- Write RLS policies for every table — never skip this
- Never expose the Supabase service role key on the client
- Generate TypeScript types regularly: supabase gen types typescript

## commit rules
Always use this keywords:
- feat -> new feature
- fix -> bug fix
- refactor -> improve code without changing behavior
- style -> formatting/UI-only small changes
- docs -> documentation
- test -> tests
- chore -> maintenance stuff
- perf -> performance improvements
- built -> built config changes
- ci -> GitHub Actions / CI stuff
Always use this structure:
- git commit -m 'KEYWORD(FILE, CHANGES OR FEATURE): EXPLANATION OF THE COMMIT'
Always git push after commiting something

## Do not suggest these — locked out of this project
- Prisma, Firebase, PlanetScale (using Supabase)
- Clerk, NextAuth (using Supabase Auth)
- React Router, react-router-dom (using Next.js App Router)
- Redux, Zustand (use React state or Supabase realtime)
- Pages Router patterns
- Any use of `any` in TypeScript

## V1 features (in scope)
- Sign up / log in with email
- Profile: display name, initials avatar and age
- Create event: name, description, type, date, time, location
- Simple RSVP: coming / not coming 
- Host guest list: attendee names, ages, total headcount
- Shareable link format: /e/[invite_code] — no auth required to view basic info
- Non-users who open a link see basic info and are prompted to sign up
- Guest can click 'coming late' and provide the expected arriving time
- Location is displayed in a Map
- voting: the host asks a question, the guest answer or vote for something
- Checklist / who brings what
- click, navigation and loading animations
- Mobile responsive

## Not in V1 — do not suggest these
- Explore page: all public events, because we want to prevent a cold-start
- Profile photo upload (using initials avatar instead)
- Gender field on profiles (legal complexity for under-18 users)
- Age limits or restrictions on events
- Join request or host approval flow
- Collaborative event type
- Party score or reputation system
- Discovery feed with filters
- External sharing buttons (WhatsApp, Instagram)
- Push notifications
- PWA
- Native mobile app
- AI features
- Public profile pages
- Party type tags

## Data model (see SCHEMA.md for full detail)
Core tables: profiles, events, rsvps, tasks (stubbed), votes (stubbed)
Key fields:
- events.invite_code: short random string for shareable links
- rsvps.status: 'going' | 'not_going'
- profiles.party_score: integer, starts at 0, used in V2

## Folder structure
- app/ - all routes
- app/e/[invite_code]/ — event page via shareable link
- components/ — shared UI components
- lib/ — Supabase client setup and utility functions
- types/ — generated Supabase types and custom types
- features/ - all big parts of the web app like login or explore

## Environment variables
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY
- SUPABASE_SERVICE_ROLE_KEY (server only — never import on client)
- API keys
Never hardcode these. Always read from process.env.

## Current state
We are in V1. Auth is complete. Supabase is connected. middleware.ts is in place. Onboarding is complete. create party page ready. My parties overview page ready (hosting/attending tabs, both clickable). Bottom nav (glass pill) ready. Event detail page /parties/[id] branches host vs guest: host sees copyable link + Bearbeiten (stub) + delete; guest sees host info + RSVP toggle. Public invite page /e/[invite_code] ready: signed-in users redirect to /parties/[id], anon users see the event and are sent to /login when they try to RSVP (after creating an account they must reopen the invite link to RSVP). RSVP working (upsert going/not_going); both statuses show under 'Ich bin Gast'. Attendees + host name are exposed publicly via SECURITY DEFINER functions. Added Profile page. Create party page is now a step-by-step flow (like onboarding): one topic per screen — Name, Beschreibung, Datum+Uhrzeit, Ort, Max. Gäste, then a finish screen with the copyable invite link. Cross (top-right) cancels to /parties; bottom progress dots show remaining questions and let you jump back to earlier ones (locked once the event is created). The event is created in Supabase on the last question; BottomNav is hidden during the flow. The Datum+Uhrzeit step now uses a custom DateTimePicker component (features/events/components/DateTimePicker.tsx): calendar on the left with month navigation (past days grayed/strikethrough, today gets a brand-pink dot, selected day gets white highlight), scrollable 15-min time slots on the right, German summary line once both are selected. Pool (voting) system complete: hosts create polls via a dedicated 'Umfragen' step in the create flow (after description, before done) or via the '+ Hinzufügen' button on the event detail page. Two pool types — 'options' (host sets fixed choices; optional text explanation per vote) and 'text_only' (guests respond with free text only). Guests vote/respond and can update or withdraw answers; all responses (names + content) visible to everyone. DB: pools, pool_options, pool_responses tables with RLS; get_pool_responses_by_event SECURITY DEFINER function exposes respondent names. Components: CreatePoolForm, PoolCard, PoolsSection (features/events/components/).
Onboarding now has a 3rd step: profile picture. User sees 'Wie siehst du aus?' screen with a clickable circle (opens file picker), preview shown in-circle after selection, 'Weiter' button appears once a photo is picked, 'Überspringen →' button always visible to skip. On skip, a random avatar_color is assigned from a fixed pool of 10 colors (constants/onboarding.constants.ts). DB: profiles.avatar_url (nullable text) + profiles.avatar_color (text not null) added; avatars storage bucket created with RLS. Progress dots in onboarding are now dynamic (reflect current step index). EventCard redesigned: full-card background image (blurred+darkened via CSS filter) with branded gradient fallback; guest cards show RSVP status chip ('zugesagt'/'abgesagt'), event title, host avatar+name (left) and days/hours countdown (right); host cards omit RSVP chip and host row; both show muted location+time line and overlapping attendee circles (max 9 + '+N' overflow). getAttendedEvents now fetches background_url, rsvp_status, and host profile (firstname, lastname, avatar_url, avatar_color) in two batch queries; getHostedEvents fetches background_url. PartiesScreen passes isHost={tab==='hosting'} to EventCard.
Mobile layout pass complete: all screens use w-full/h-dvh/min-h-dvh instead of w-screen/h-screen; BottomNav uses safe-area-inset-bottom via .bottom-safe-nav utility; content pages use .pb-safe-nav to clear the nav + home indicator; viewport meta exports viewportFit='cover'; html/body have overflow-x:hidden; EventDetail hero uses h-[45dvh].
PartiesScreen event lists (both 'Ich hoste' and 'Ich bin Gast') are now sorted ascending by event_date (soonest first) client-side; getAttendedEvents in events.service.ts also sorts server-fetched rows the same way, and both services now select events.max_guests. EventCard has a new `featured` prop — PartiesScreen passes featured={index === 0} so the next upcoming event gets a bigger layout: large host avatar (guest view only) + zugesagt/abgesagt/vielleicht pill badge (RsvpBadge) top row, title + host name, then a 3-stat row (📅 Datum, ⏳ Tage/Stunden countdown, 👥 attendee/max. Gäste). Non-featured cards keep the compact layout: title + small host-avatar row on the left, badge + single Tage stat stacked top-right, attendee circles inline in the row (featured card still hangs its circles below the card edge, outside overflow-hidden). Host view (isHost=true) hides the avatar/badge/host-name row on both variants. RsvpBadge now covers all three RsvpStatus values via RSVP_BADGE_CONFIG (going ✅ zugesagt/success, not_going ❌ abgesagt/warning, maybe 🤔 vielleicht/maybe color token); EventWithCount.rsvp_status is typed as RsvpStatus | null and getAttendedEvents no longer force-casts RSVP rows to only 'going' | 'not_going', so maybe-status RSVPs now render correctly in "Ich bin Gast".
The host is now always included in the attendee circles shown by EventCard, on both tabs: getHostedEvents fetches the host's own profile and prepends it to attendees (+1 to attendee_count) so a host sees themselves even with zero RSVPs; getAttendedEvents prepends the host (using the real avatar from get_event_host, deduped if the host already has an rsvp row) so guests always see the host in the guest list too. host_avatar_color/host_avatar_url in getAttendedEvents now use get_event_host's real avatar_color/avatar_url instead of the old hash-based placeholder color and hardcoded null.
BottomNav now has explicit z-30 (was unset/auto) so it stacks above every screen's z-10/z-20 content layers regardless of DOM order; dropdown overlays (Combobox/Selectbox, z-50, onboarding-only) remain unaffected.
ProfileScreen's 5 setting-row circles (right side) are now transparent with a centered grey chevron-right icon (text-hint) instead of solid black circles.
All 5 ProfileScreen rows are now clickable Links to dedicated sub-pages (design untouched): /profile/name (EditNameScreen — edit firstname/lastname), /profile/age (EditAgeScreen — day/month/year Selectbox prefilled from birthday, day options clamp to the days-in-month), /profile/picture (EditPictureScreen — pick+upload a new avatar to the existing avatars bucket, same validation as onboarding's ProfilePictureForm), /profile/password (EditPasswordScreen — thin wrapper adding a back button around the existing features/settings/change-password.tsx form, which already does 2x password entry + usePasswordValidation strength check), /profile/legal (LegalScreen — static placeholder, "Impressum und Datenschutzerklärung folgen in Kürze"). Each screen re-guards the session client-side (redirect to /login if none) like ProfileScreen already did. profile.service.ts gained updateProfileName/updateProfileBirthday/updateProfileAvatar (plain `.update().eq('id', userId)`, protected by the existing "Users can update their own profile" RLS policy). BottomNav now also hides on /profile/[a-z]+ sub-pages, matching the /parties/[id] pattern. Note: features/settings/screens/ChangePasswordScreen.tsx is pre-existing dead code (unstyled, unused) — left untouched.
Featured EventCard's 3-stat row now hides the 👥 max. Gäste pill entirely when event.max_guests is null (no fallback to attendee-count-only display). That row is now a grid-cols-3 (not flex justify-between) with per-item justify-self-start/center/end, so the 📅 Datum pill stays pinned left (aligned with the title) and the ⏳ countdown pill stays dead-center regardless of whether the max. Gäste pill renders.
CreateEventScreen's 'Hintergrundbild' step (empty state, before a file is picked) now matches a dashed-border upload-box design: rounded dashed box (border-border-input) with a circular icon badge (bg-background-tertiary) containing a cloud-upload SVG, bold 'Datei auswählen oder hierher ziehen' headline, 'JPG, PNG bis 10 MB' subtext, and a pill-shaped 'Datei durchsuchen' button — all inside one clickable label wrapping the hidden file input. Once a file is picked it still shows the aspect-video image preview as before (unchanged); the caption text below the box was removed since the new box carries its own copy.
Create-party flow buttons are now consistent across steps: every white primary button says 'weiter' (the separate 'Überspringen →' link on the background step and the 'erstellen' label on the pools step are gone). The button is disabled only on required steps until valid (name, when, location — via canContinue); optional steps (description, guests, background, pools) leave it always enabled. The final 'done' screen keeps its own 'Fertig' label/action since it exits the flow rather than advancing to another question.
The event row is no longer created after the 'Max. Gäste' step — the whole create-party flow (name → when → location → description → guests → background → pools) is now collected purely in local state and nothing touches Supabase until the 'weiter' button on the last 'Umfragen' step is pressed. That single handleFinish() call inserts the event, then (if present) uploads the picked background file and attaches it, then inserts each locally-drafted pool + its options against the new event id, then reveals the 'done' screen with the invite link. Background picking (handlePickBg) is now purely local (preview only, no upload) and CreatePoolForm no longer talks to Supabase either — it just returns a local PoolDraft (id via crypto.randomUUID(), question, description, options) via onCreated; both are only persisted inside handleFinish. Side effect: since nothing is written until the final step, users can freely jump back through StepProgress dots and edit any earlier answer (including background/pools) right up until submission, and cancelling (✕) mid-flow no longer leaves an orphaned event row in the database.
DateTimePicker's time-slot list order is reversed (only the list order — the 15-min slot values themselves are unchanged): 00:00 stays pinned at the top, then it drops straight to 23:45 and counts down (23:30, 23:15, … 00:15) to the bottom.
--color-background-main (and the matching html background-color in globals.css) changed from #09090B to pure #000 — true black instead of near-black everywhere the token is used.
The decorative concentric-circles-in-the-corners + full-page `backdrop-blur-[80px]` overlay (the `CIRCLES` array pattern) is removed from every screen that had it: AuthScreen, app/(auth)/forgot-password, OnboardingScreen, PartiesScreen (was already commented out there, now fully deleted), ProfileScreen (dead const only), CreateEventScreen (dead const only), and the unused EventBackground component. Those screens now render on plain `bg-background-main` with no decorative background layer. The per-event blurred background image (EventDetailScreen/InviteScreen, driven by `event.background_url`) and the `backdrop-blur-xl` glass cards (EventInfoCard, back-button pill) are untouched — different feature, out of scope.
EventCard was rewritten from scratch to a much simpler design: no more RSVP badge, host avatar, countdown stat row, or overlapping attendee circles. It's now just an image card (event.background_url as a plain, undarkened object-cover img, or a flat bg-secondary grey box if there's none) with the title and (guest view only) "von {Host}" printed below the card as plain text — nothing is overlaid on the image anymore. The `featured` prop only controls aspect ratio now: featured (first upcoming event) is aspect-[2/1] full width; all other cards are aspect-square. PartiesScreen renders the first event in currentList as the lone featured card, then the rest in a 2-column grid (grid-cols-2 gap-3) — no more mapping every event through the same flex-col list with an index === 0 check.
globals.css's whole color/text-size token system was replaced with a Figma-derived scale (this superseded an earlier, larger palette that had `headline`/`subheadline`/`hint`/`placeholder`/`label`/`input`/`button`/`background-input`/`background-spacer`/`background-button`/`background-profilpicture`/`background-icon`/`glass`/`glass-strong`/`warning`/`success`/`maybe`/`border`/`border-input`/`border-button` — none of those exist anymore, so any leftover classNames using them are unstyled; `bg-background-main` in PartiesScreen.tsx is one such stale reference, left as-is since it wasn't part of the requested change). Current tokens: colors `--color-main/secondary/tertiary/quaternary` (base surfaces, secondary/tertiary are translucent rgba now, not solid), `--color-arrow` (used for the empty-state doodle arrow's stroke), `--color-heading/subheading/label-large/label-small/body` (text roles); sizes `--text-heading-1..4/subheading-1/label-1/label-2/body-1/button` (all under `--text-*`, distinct from the same-named `--color-*` roles to avoid Tailwind v4 silently dropping the font-size half when a `--color-foo` and `--text-foo` share a key — verified empirically via a throwaway Tailwind compile).
PartiesScreen's "Gastgeber" tab now shows a dedicated empty state when the host has zero events (the "Gast" tab keeps the old plain-text empty message): a two-box skeleton card mockup (bg-tertiary/bg-secondary translucent boxes + skeleton text bars) hinting at what an event card looks like, a headline "Starte jetzt mit "{profile.firstname}"" + subtext, and a hand-drawn SVG doodle arrow (open hook top-left, one smooth bulge, then a straight vertical run into the arrowhead so the curve never crosses the arrowhead strokes) whose tip sits exactly on the horizontal center of its 130px-wide box — pointing down at the (horizontally centered) "+" in the bottom nav. The outer PartiesScreen wrapper had `overflow-hidden` removed so the page can actually scroll if this empty state is taller than the viewport.
EventDetailScreen's back button is now a solid `bg-secondary` circle (h-11.25 w-11.25, matching the create-event "+" button style) instead of the old glass/blur pill; its icon is a bold chevron (`polyline points='9 6 15 12 9 18'`, strokeWidth 4, rotated 180° via `transform='rotate(180 12 12)'` so it points left/back). The same chevron un-rotated (`ChevronRightIcon`) is now used as a trailing affordance next to the "Location"/"Umfragen"/"Gäste" section headings. EventDetailScreen gained a horizontally scrollable stats row (`overflow-x-auto scrollbar-none`, iOS momentum scroll via `[-webkit-overflow-scrolling:touch]`) right under the title/description divider, showing Datum/Uhrzeit/Max. Gäste (hidden if null)/zugesagt/vielleicht/abgesagt as label+value pairs (vielleicht sits BETWEEN the other two, matching the ••• dropdown's order; both screens build this array identically and must stay in sync) (`text-label-2 text-label-small` label, `text-label-1 font-semibold text-label-large` value); `EventDetail`/`getEventById` now also select `max_guests`. The Location block splits `event.location` (stored as `"<address>, <city>"`, see `CreateEventScreen.tsx` handleFinish) back into an address line and a "in {city}" line by cutting at the last comma. The "Umfragen" and "Gäste" sections are now conditionally rendered — EventDetailScreen fetches `getEventPools` itself (in addition to `PoolsSection` doing its own internal fetch) purely to know whether `pools.length > 0` before showing the Umfragen block at all; the Gäste block only renders when `attendees.length > 0`. `AttendeeList` was redesigned: plain rows separated by hairline dividers (no more per-row bordered cards), bigger (56px) avatar, bold name + "{age} Jahre alt", and a status pill (✅ zugesagt / 🤔 vielleicht / ❌ abgesagt) using three new semi-transparent globals.css tokens — `--color-success` (#2F9E44), `--color-maybe` (#A6901F), `--color-warning` (#8B2E2E) — added specifically for these pills since the old success/warning/maybe tokens were removed in the token-system rewrite.
The "Max. Gäste" stat on BOTH the detail page and the invite page now shows occupancy rather than the bare cap — `${counts.going}/${event.max_guests}`, e.g. `45/50` — and `features/events/components/CapacityWarning.tsx` renders a banner under the stats row once the event is nearly full. The box itself was later extracted to `components/shared/WarningBanner.tsx` — the app's ONE warning surface, now shared by the capacity notice and the password screen — a `rounded-xl border-border border-warning/60 bg-warning/15` box (`border-border`, the app's 0.75px hairline width, not Tailwind's 1px `border`) with a lucide `TriangleAlert` (confirmed present in lucide-react 1.28.0; `AlertTriangle` is its deprecated alias) and red text, matching the reference Arthur sent. The threshold is the last 10% of the places (`maxGuests - going <= maxGuests * 0.1`) and lives ONLY in that file, exported as `isNearlyFull(going, maxGuests)`, because the stats row uses the same condition to paint its value `text-warning` instead of `text-label-large` — the `stats` array gained an optional `warn` flag for that. German copy handles all three cases: `Keine Plätze mehr frei.` (0), `Nur noch 1 Platz frei.` (1), `Nur noch N Plätze frei.` (N). The banner is guarded by `!statsLoading` so it cannot flash during load, and it renders for hosts and guests alike. Both screens got the identical change, per the existing rule that their stats arrays must stay in sync.
ProfileScreen was redesigned from Arthur's mockup and, in the process, off the dead token system it was still written against (`bg-background-main`, `bg-background-secondary`, `text-headline`, `text-hint` — its rows had been rendering colorless). It now shares the parties page's background: `<FloatingEmojis active={!loading} />` on `bg-main`, with the same veil, under a `relative z-10` column. Top block is a 125px avatar (`h-31.25 w-31.25`, real photo or initials on `avatar_color`, with the usual `avatar_url ? 'transparent' : color` guard so no colored ring leaks around the photo), the full name at `text-heading-3` bold and the auth email at `text-subheading-1` below it. Below that, iOS-style grouped cards (`rounded-[25px] bg-secondary backdrop-blur-xl overflow-hidden`, `gap-3` between groups — the radius is exactly HALF a row's 50px height, which Arthur specified as 50% per corner, so a one-row card comes out a full pill while the two-row card keeps identical corners without becoming one; do not swap it back to a `rounded-*` step) whose rows are all `h-12.5` (50px, the height Arthur specified) with an emoji, a `text-button` label, and either a value or a lucide `ChevronRight`: card 1 holds Name and Alter TOGETHER, each showing its current value instead of a chevron and separated by an inset 0.75px hairline (built with the AttendeeList spacer trick — an invisible `w-6` + `ml-3` so the line always starts under the label); the groups were then restructured to Arthur's spec into TWO cards: one holding Name, Alter and Profilbild, and one holding Passwort, Account verwalten and Rechtliches (all three chevron links). They briefly carried 'Account' / 'Allgemeines' captions above them; Arthur had those removed, so the cards stand on their own. Abmelden and Account löschen are no longer rows here: they moved to their own sub-page behind the 'Account verwalten' row, `features/profile/AccountScreen.tsx` + `app/(dashboard)/profile/account/page.tsx`, which mirrors the other sub-pages (back button to /profile, one card with the two rows, delete in `text-warning`) and carries `handleSignOut` and the `delete_self` call. Name and Alter were briefly EDITED IN PLACE (inline inputs, focus-to-clear, an Abbrechen/Speichern bar, and a `birthdayForAge()` helper that shifted only the birth year) — Arthur had all of that REVERTED: both rows are plain `Link`s again, showing their current value to the LEFT of a chevron exactly like Profilbild, and navigating to their own edit page. `app/(dashboard)/profile/name`, `app/(dashboard)/profile/age`, `EditNameScreen.tsx` and `EditAgeScreen.tsx` had been deleted during that experiment and were restored from the commit, then patched off the dead token system they were still written against (`bg-background-main`/`text-headline`/`text-label`/`bg-background-input`/`text-input`/`placeholder:text-placeholder`/`bg-background-button` → `bg-main`/`text-heading`/`text-subheading`/`bg-secondary`/`text-button text-heading`/`placeholder:text-label-small`/`bg-sheet text-sheet-heading`), since users are routed to them again and their inputs had been rendering colorless. `if (loading) return null` (a blank page) was replaced by skeletons matching the layout: avatar circle, name bar, email bar and two `h-37.5` cards, one per group.
`/home` IS GONE (`app/(dashboard)/home/page.tsx` deleted). It was a placeholder card, still written in the dead token system, that happened to be the landing page for SIX code paths — `app/page.tsx`, the OAuth callback's fallback, SignInForm's success, ChangePasswordForm's success, AuthScreen's password-reset exit and OnboardingScreen's exit — while also holding the app's only sign-out and only `delete_self` call. All six now go to `/parties`, and the two account actions moved onto ProfileScreen. Verified: `/home` returns 404, `/`, `/parties`, `/profile`, `/login` and `/profile/legal` all 200, and `/callback` with no code redirects to `/parties`.
Every profile sub-page (`EditNameScreen`, `EditAgeScreen`, `EditPictureScreen`, `EditPasswordScreen`, `LegalScreen`) had its own hand-rolled `BackIcon` `<svg>`; all five now use lucide `ChevronLeft` in the same `bg-secondary` circle as `AccountScreen` and the event pages, so the back button is identical everywhere. Their remaining dead tokens went with it (`text-headline`, `text-hint`, `bg-background-main`, `bg-background-button` on EditPictureScreen's save button → the white `bg-sheet` pill). This is the change that prompted the icons rule now at the top of this file: lucide only, plus Apple emojis, no drawn substitutes. Note the rest of the app still contains hand-rolled `<svg>`s outside features/profile (BottomNav, the create-event flow, the invite sheet's brand marks) — only the brand marks and true illustration are meant to stay.
EditPasswordScreen reports its problems in that same `WarningBanner` rather than a bare red line, and LIVE rather than on save: a mismatch (`confirm` non-empty and the two differing) outranks the `usePasswordValidation` strength hint, because it is the one that also DISABLES speichern. The `alertError` calls in `handleSave` stay as a backstop but are no longer reachable through the UI.
Rechtliches is now a LIST, not a dead end: its two rows (Impressum, Datenschutz) lost their 'folgt' text and became chevron `Link`s to `/profile/legal/impressum` and `/profile/legal/datenschutz`. Both routes render the same `features/profile/LegalTextScreen.tsx` — one screen taking `title` and a `paragraphs` string array — and the copy lives in the route files as a `PARAGRAPHS` const, clearly marked as PLACEHOLDER to be swapped for the real documents before launch. That is also why `SettingsPage` gained a `backHref` prop: these sit one level deeper, so their back button returns to `/profile/legal` rather than jumping home to `/profile`. BottomNav needed no change — its `^/profile/.+` rule already hides it on nested paths.
CREATE-EVENT FLOW was rebuilt from eight mockups. It is now nine steps — name, date, time, location, description, guests, background, pools, done — each rendered through `features/events/components/CreateStepLayout.tsx`: `<FloatingEmojis active />` + veil on `bg-main`, ✕ top-LEFT, an optional `skip ›` top-right, the headline near the top, and the answer card + white primary button + step dots stacked at the bottom. The card/row classes come from the NEW `components/shared/Card.tsx`, which `SettingsPage` now re-exports — so the create flow and the profile pages are the same components by construction, not by copying. `StepProgress` lost the disabled state: EVERY dot is tappable (until the event exists), and it is dots only, no pill.
TWO MIGRATIONS came with it. `events.ends_at timestamptz` (nullable) stores the end time from the new two-row time step; when the end is earlier than the start the date is rolled forward a day, because the party runs past midnight. `pools.allow_multiple boolean default false` backs the 'mehrere Antworten erlauben' toggle, and a THIRD migration made it real: `pool_responses` lost its `(pool_id, user_id)` unique constraint, replaced by a partial unique index on `(pool_id, user_id, option_id)` where option_id is not null, plus one on `(pool_id, user_id)` where it IS null so text answers stay single. `upsertPoolResponse` therefore deletes-then-inserts (a plain upsert would append a second row now), and `addPoolResponse`/`removePoolResponse` toggle one option at a time for multi-answer polls. PoolCard branches on `pool.allow_multiple`: it keeps the in-place optimistic merge for single answers and rebuilds my rows from the selected set for multi.
The date and time steps use `components/shared/WheelSheet.tsx`, extracted from BirthdayPicker so all three wheels are one implementation (BirthdayPicker is now 59 lines of configuration). `EventDateSheet` clamps the day when a shorter month is picked; `EventTimeSheet` is hours plus five-minute steps. The location step has a typable Adresse row with a white search circle that opens `AddressSearchSheet` — OpenStreetMap Nominatim, no key, no billing, debounced 400ms with an AbortController so a slow early request cannot overwrite a later one, limited to de/at/ch. The background step offers an upload box OR eight ready-made photos in `public/backgrounds/bg-1..8.jpg` (1200x800, q68, 1.12MB total) — picking a preset writes its path straight into `background_url` with no upload — and it has NO skip, a background is required. Polls: the step lists what has been added, an 'Umfrage hinzufügen' row opens `PoolDraftForm` (Frage, two or more options with removal beyond the second, the multi-answer switch, Details), and skip on that step finishes the event. `DateTimePicker.tsx` and `CreatePoolForm.tsx` were deleted — both superseded.
AVATARS all go through ONE component now, `components/shared/Avatar.tsx` (`size`, `url`, `color`, `firstname`, `lastname`, `className`), because they had drifted badly across seven call sites. Three real bugs it fixed: (1) the host avatar on EventDetailScreen AND InviteScreen, and the profile avatar in PartiesScreen's header, were hardcoded `bg-secondary` — they IGNORED the stored `avatar_color` entirely, so a user who picked a colour still saw grey there; (2) the initials were 20–22% of the circle's diameter, which is why they looked lost — the component now derives `fontSize` from `size * 0.4`, the iOS proportion, for every avatar in the app; (3) the weight was `font-bold`/`font-semibold`/none depending on the screen and is now always `font-semibold` (the profile area's 'semibold is for buttons' rule takes avatars as an exception: regular weight is unreadable in a 24px voter circle). The `url ? 'transparent' : color` guard against a colour ring leaking around a photo now lives in one place instead of being repeated. Call sites and sizes: PoolCard voters 24, event/invite host 25, ProfileScreen row thumbnail 30, PartiesScreen header 45, AttendeeList 50, EditPictureScreen grid 90, ProfileScreen 125, EditPictureScreen preview 175. `getInitials` is no longer called anywhere outside the component.
MOTION PASS over the profile area, on top of the shared `cubic-bezier(0.32,0.72,0,1)` easing used everywhere else: settings ROWS highlight on press (`transition-colors duration-150 active:bg-white/10`) rather than scaling — a row scaling inside a card tears away from the divider above it — while standalone controls keep `active:scale-95`. `saveButtonClass` transitions `[transform,background-color,color]` so the disabled state FADES between `bg-sheet` and `bg-sheet/40` instead of snapping. EditPictureScreen's big circle presses with `active:scale-95` and its pencil badge follows via `group-active:scale-95` on a `group` wrapper, the chosen photo cross-fades in, and the blue tick scales in with `.animate-fade-in-up`. ProfileScreen fades in ONLY its header (avatar, name, email) — the cards below carry `backdrop-blur-xl`, and an ancestor animating opacity would flatten their blur for the animation's whole duration, which is the same trap the parties empty state hit. For the same reason BirthdayPicker does NOT slide up on a transform: its sheet is revealed by the `grid-rows-[0fr]` → `[1fr]` height trick (with a `shown` flag flipped on the frame after mount so there is a state to animate from) while the scrim fades its opacity separately — a transform on the sheet would leave it flat and grey until it settled. The 'Account verwalten' row's emoji is now ⚙️.
LOADING has two shapes now. SKELETONS (`.skeleton`) stand in for content whose layout is known — they now also cover the profile sub-pages, which previously flashed empty fields while the profile loaded: EditNameScreen (one `h-25` card), EditAgeScreen (one `h-12.5` row) and EditPictureScreen (the 175px circle plus nine 90px circles with their radios, 19 shimmer nodes). EditPasswordScreen has none on purpose — it loads nothing, its fields start empty. SPINNER (`components/shared/Spinner.tsx`, built from Arthur's reference) is for waits with no shape to stand in for: it replaces the label inside a button while the write is in flight, on all four profile save buttons and on the create-event flow's final 'weiter' — the longest wait in the app, since that one insert also uploads the background and writes every poll. It is twelve blades on a circle, each running `.spinner-blade` (opacity 1 → 0.15 over 1s, linear, infinite) with a NEGATIVE stagger of i/12 seconds so the bright blade appears to travel round; geometry is derived from a single `size` prop (blade 10% x 26%, pushed out by 32%), and the colour is `bg-current`, so it inherits the button's text colour and needs no prop on either the white pills or the dark ones. `prefers-reduced-motion` freezes it at half opacity rather than hiding it, since it is the only signal that something is happening. Note the buttons that host it gained `flex items-center justify-center` — a plain text button does not centre a child element.
BLUR RULE, APP-WIDE: every TRANSLUCENT surface carries `backdrop-blur-xl` — cards, inputs, buttons, back buttons, the bottom nav pills, the create-event and onboarding controls, DateTimePicker, CreatePoolForm, Selectbox, PoolCard, EventCard. `grep -rn 'bg-secondary\|bg-tertiary\|bg-quaternary\|bg-white/[0-9]\|bg-black/[0-9]\|bg-main/[0-9]' features components app | grep -v backdrop-blur` must return only COMMENT lines. Surfaces that already had a deliberate blur were left at their own value and NOT flattened to xl: BirthdayPicker (`2xl` sheet, `xs` scrim) and FloatingEmojis' veil (`xs`). The event pages' header buttons and ••• panel were the exception and have since gone to `xl` at Arthur's request — `iconButtonClass`, `menuContainerClass`, the ••• trigger and the RSVP rows inside the panel on BOTH EventDetailScreen and InviteScreen, which must stay identical to each other. InviteScreen's RSVP GATE BAR went with them — the red absagen circle, the green zusagen pill and the yellow vielleicht circle were the last `backdrop-blur-md` in the app and are now `xl` too, so `grep -rn 'backdrop-blur-md' features components app` returns nothing. Opaque surfaces get nothing, since a backdrop-filter behind something you cannot see through only costs GPU. IMPORTANT detail Arthur caught: a DISABLED button must not fade with `disabled:opacity-40` — `opacity` fades the element together with its already-blurred backdrop, so the unblurred page shows straight through. `saveButtonClass` now uses `disabled:bg-sheet/40 disabled:text-sheet-heading/60` instead, keeping the blur alive while the pill goes translucent. The other `disabled:opacity-*` buttons in the create-event flow and onboarding still have the old pattern.
TYPE WEIGHT RULE for the whole profile area, set by Arthur: `font-bold` appears EXACTLY ONCE — the user's name on ProfileScreen, the only place it is allowed. Everywhere else on the profile page and every page it leads to, emphasis is `font-semibold`, and ONLY in two places: the `SettingsPage` title and `saveButtonClass` (speichern, Abmelden, Account löschen). Row LABELS are deliberately NOT semibold — they were made semibold once and Arthur had it reverted, so `rowLabelClass` in both copies (SettingsPage's and ProfileScreen's) stays at the default weight. Values, placeholders and the avatar initials do too, and BirthdayPicker's selected row signals itself with colour and the band rather than weight. `grep -rn 'font-bold' features/profile` must return exactly one hit; if it returns more, the rule has been broken. NOTE this rule is profile-scoped as stated — `font-bold` is still used on titles in auth, onboarding, the create-event flow, PartiesScreen ('Events') and the event pages, and Arthur has not asked for those to change.
EditPictureScreen offers TWO ways to have an avatar, both on ONE screen, stacked: a 175px circle at the top, the 3x3 colour grid under it, the save button under that. (A sliding Profilbild/Initialien tab pill was tried first and Arthur removed it.) The circle is the file input — it shows the current photo, the preview once a file is picked, the initials on the selected colour, or a lucide `User` glyph when there is none of those — with a white `bg-sheet` badge holding a lucide `Pencil` at its bottom-right, which is decoration on top of the label rather than a second control. The grid is the NINE `AVATAR_COLORS`, each a 90px circle showing the user's initials, with a radio underneath — `bg-link` plus a white lucide `Check` when picked. The two are MUTUALLY EXCLUSIVE in both directions: picking a file clears the selected colour, picking a colour clears the file and its preview, and the big circle previews whichever won. Saving follows what is selected: a photo uploads to the `avatars` bucket and sets `avatar_url`; a colour calls the new `updateProfileAvatarColor()`, which sets `avatar_color` AND NULLS `avatar_url`, because the two are alternatives and every avatar in the app falls back to initials exactly when `avatar_url` is null. A user already on initials starts with their colour marked. `AVATAR_COLORS` was trimmed from ten to nine (the grid is 3x3, and the dropped one was `#161BFA`, the brand blue this file already flags as near-unreadable) and now lives as the single source for both this picker and onboarding's random assignment, so a colour handed out at signup is always one the user can pick again. The upload-box version of this screen (a 2:1 dashed box above the save button) and an eight-photo preset grid were both earlier attempts; the eight cropped photos that fed the second one lived unreferenced in `public/avatars/` (`party-1..8.jpg`, 1000x1000, the same source photos as `public/backgrounds/` cropped square) and have now been DELETED — they were never tracked by git, nothing imported them, and Next was still serving them at `/avatars/party-N.jpg` and shipping them with every deploy. `SettingsPage` has a `fill` prop for this page: the container becomes `min-h-dvh` and the children get `flex-1`, so the content starts at the top rather than hugging the bottom like the single-card pages. The save button does NOT ride `mt-auto` here — that pinned it to the bottom of the viewport and left a large dead gap under the grid, which Arthur asked to close, so it simply follows the grid with `mt-4`. It is also deliberately not `position: fixed`, which would cover the last row of circles on a small screen and need bottom padding to compensate.
All profile sub-pages were then redesigned from four mockups onto ONE shared shell, `features/profile/components/SettingsPage.tsx` (used 6x — EditName, EditAge, EditPassword, EditPicture, Legal, Account). It owns the whole frame: `<FloatingEmojis active />` + veil on `bg-main` exactly like the parties and profile pages, a `bg-secondary` circle back button with lucide `ChevronLeft` at the left, the page title centred at `text-heading-3`, and — the defining part of the mockups — `mt-auto` on the content, so the card and its lowercase white `speichern` pill sit at the BOTTOM of a `h-dvh` column above the home indicator (`pb-safe-rsvp`). It also exports the shared `cardClass` / `rowClass` / `rowLabelClass` / `rowValueClass` / `rowInputClass` / `saveButtonClass` / `RowDivider`, so the 50px rows and the `rounded-[25px]` cards cannot drift from ProfileScreen's. Name = two rows (Vorname/Nachname) with the input right-aligned as the row's value; focusing either field CLEARS it so a new name can be typed without deleting the old one (the stored value stays visible as the placeholder, and blurring an empty field restores it), and both Name and Alter keep a copy of what is stored so `speichern` stays DISABLED until something actually differs — Arthur asked for both explicitly; Passwort = the same two-row shape (neues Passwort / Passwort wiederholen) and it now does its own `supabase.auth.updateUser` with `usePasswordValidation`, so it no longer wraps `ChangePasswordForm` (that component still serves AuthScreen and /forgot-password); Profilbild = the 160px preview above one 'Bild' row; Rechtliches = two placeholder rows; Account is the exception to the row pattern: its two actions are STANDALONE white pills (`saveButtonClass`), not rows inside a shared card, and they use lucide icons rather than emojis — `LogOut` for Abmelden and `Trash2` for Account löschen, whose label and icon are `text-warning` red on the white pill. Arthur asked for exactly that: they are actions, not settings, and the destructive one should not share a box with the harmless one.
The birthday is picked with `features/profile/components/BirthdayPicker.tsx`, a hand-built iOS-style WHEEL (Arthur's mockup is the native iOS picker; a native `<input type='date'>` would look right only on iOS Safari and cannot be styled, so it is custom). The sheet rises from the bottom over a `bg-main/50 backdrop-blur-xs` scrim and is TRANSLUCENT WHITE: `bg-sheet/75 backdrop-blur-2xl` (it was briefly recoloured dark to `bg-secondary` and Arthur put it back — white, but see-through enough that the blur actually shows). It holds three CSS scroll-snap columns (day with a trailing '.', German month names, years) with one `bg-button-secondary/80` selection band behind their centre, rows in `text-sheet-heading` when selected and `text-sheet-body` when not, and a `mask-image` fading them out at both edges. This is the app's second light surface after the invite page's sign-up sheet, which is why those tokens exist. Selection is read from `scrollTop / ITEM_HEIGHT` (36px) throttled to one rAF per scroll. TWO traps are already handled: (1) a `userScrolling` ref set on every scroll event and cleared 150ms after the last one, because the effect that scrolls a column to its selected row would otherwise yank the list back mid-flick — every index change the user's own scrolling causes looks identical to one from outside; (2) a day that no longer exists in a shorter month is clamped down (31 March → February gives 28/29). The page also locks `document.body` scroll while the sheet is open.
Login/signup (AuthScreen + SignInForm/SignUpForm/VerifyOtpForm/change-password) and onboarding (OnboardingScreen + PersonalDataForm/BirthdateForm/ProfilePictureForm), plus the shared Selectbox dropdown, were functionally broken — every text/input/button class still referenced color tokens from the pre-rewrite design system (`text-headline`, `bg-background-input`, `text-input`, `bg-background-button`, `text-hint`, `text-label`, `bg-glass`, `text-placeholder`, `bg-background-tertiary`, `bg-background-main`) that no longer exist, so typed text and button labels rendered invisible (no color = black-on-black). Fixed by swapping every occurrence for its current equivalent (`text-heading`, `bg-secondary`, `bg-tertiary`, `text-label-small`, `text-subheading`, `bg-main`) — purely functional, no new visual design.
The same dead-token problem existed in the create-party flow — CreateEventScreen.tsx (incl. the ✕ cancel button, which used the also-dead `border-glass`/`bg-glass`/`backdrop-blur-xl` combo and is now a plain `bg-secondary` circle like the rest of the app's icon buttons), DateTimePicker.tsx (calendar/time-slot picker), and CreatePoolForm.tsx (the in-flow poll creator) — fixed the same way, same token mapping.
EventDetailScreen's hero (was a `bg-green-500` placeholder) now shows `event.background_url` as a full-bleed `object-cover` image (flat `bg-secondary` box fallback if none), with a `bg-gradient-to-t from-main to-transparent` overlay pinned to its bottom 128px so it blends seamlessly into the black content section below (no visible seam, since the gradient's end color matches the content div's `bg-main`); back/copy-link buttons sit in a `relative z-10` wrapper on top. Host-only copy-link button added top-right of that header (mirrors the back button's `bg-secondary` circle style), reusing the pre-existing `handleCopy`/`shareLink`/`copied` state — swaps to a ✓ for 2s after copying.
AttendeeList's row divider is now alignment-proof: instead of a hardcoded `pl-17` guess, it's an invisible `h-12.5 w-12.5` spacer (matching the real avatar's exact size/gap) followed by a `flex-1` line, so the divider always starts exactly under the name/age column and runs to the row's full right edge regardless of avatar size changes.
PoolCard was redesigned to match a Figma reference: no outer card border, bold question + conditional description, each option is now a full-width rounded pill (`bg-tertiary` selected / `bg-secondary` unselected) with a radio circle on the left (filled when voted) and an overlapping avatar stack of that option's voters on the right (photo or initials-on-avatar_color, capped at 3 + `+N` overflow) instead of the old percentage badge. Needed `avatar_url`/`avatar_color` on `PoolResponse` (events.types.ts) and on the `get_pool_responses_by_event` SECURITY DEFINER function (migration `20260801225328_add_avatar_to_pool_responses_function.sql`) — previously that RPC only exposed respondent names. PoolsSection's inter-poll spacing bumped (`gap-2` → `gap-4`) since cards no longer have borders to separate them.
Fixed a visual bug affecting every avatar circle that has BOTH a real photo and a random `avatar_color`: the `style={{ backgroundColor: avatar_color }}` was applied unconditionally, so on any circle where the `<img>` doesn't pixel-perfectly cover the rounded div (object-cover + rounded-full clipping isn't exact), a thin ring of that random color leaks through around the photo's edge. `ProfileScreen.tsx`/`EditPictureScreen.tsx` already guarded against this (`avatar_url ? 'transparent' : avatar_color`); applied the same guard to the three spots that didn't — `PoolCard.tsx` (voter avatar stack), `AttendeeList.tsx`, and `HostRow.tsx`.
EventDetailScreen's Location/Umfragen/Gäste sections are now collapsible: `openSections` state (`{ location, polls, guests }`, all `true` by default) toggled via `toggleSection(key)`; each headline row is now a full-width `<button>`, and `ChevronRightIcon` is wrapped in a `<span>` that gets `rotate-90` (+ `transition-transform duration-200`) when its section is open — pointing down when expanded, right when collapsed — with the section content conditionally rendered on that same state.
Guests (never hosts) now see a "•••" button (`MoreIcon`, same `bg-secondary` circle as the back/copy buttons) in the header's top-right, in the spot the host's copy-link button occupies for hosts. Clicking it toggles a `menuOpen` dropdown — a `w-56.25 rounded-3xl bg-quaternary/50 backdrop-blur-xl` panel (translucent + blurred on purpose, over the hero image) closed by a `fixed inset-0` invisible backdrop — listing zugesagt/vielleicht/abgesagt (`RSVP_MENU`, reuses the existing `handleRsvp`, current status highlighted with a `bg-tertiary` pill) then, after a divider, a red "🗑️ Löschen" row (`text-warning`) that calls the new `handleLeaveEvent` — deletes only that guest's own `rsvps` row (new `deleteRsvp(eventId, userId)` in events.service.ts, backed by the pre-existing `rsvps_delete_own` RLS policy) and redirects to `/parties`, never the event itself.
Dead-code cleanup pass (baseline before: `tsc` clean, `eslint` 77 errors / 36 warnings; after: `tsc` clean, 0 errors / 14 warnings). Deleted 5 unreferenced files: `features/events/EventDetailScreen 2.tsx` (untracked editor duplicate), `features/events/components/EventBackground.tsx` and `EventInfoCard.tsx` (both zero importers and both still written against the pre-rewrite token system), `features/settings/screens/ChangePasswordScreen.tsx` (the unstyled dead twin previously noted here), and `components/shared/Combobox.tsx` (zero importers — `Selectbox` is the one actually used by BirthdateForm/EditAgeScreen). Deleted unused code inside surviving files: `EventDetailScreen.tsx` lost the `HostRow`/`RsvpButtons` imports, the `deleting` and `descExpanded` state, the local `getCountdown()` + its `value`/`unit`/`formattedDate` results, and `handleDelete`; `ProfileScreen.tsx` lost `handleSignOut`; `Selectbox.tsx` lost the computed-but-unused `isHighlighted`; `pools.service.ts` lost `deletePool`/`deletePoolResponse`; `auth.types.ts` lost `AuthStep`.
TWO FEATURE GAPS this cleanup made explicit — the code was there but wired to nothing, and is now gone: (1) **hosts cannot delete an event.** `deleteEvent(eventId)` is deliberately KEPT in events.service.ts as the rebuild hook; the UI needs a host version of the ••• dropdown carrying a Löschen row (mirror `handleLeaveEvent`, but call `deleteEvent`). (2) **there is no sign-out on the profile page.** BOTH gaps are CLOSED now — see the host ••• dropdown further down, and the Abmelden / Account löschen rows on ProfileScreen.
Structural tidy-up in the same pass: `features/settings/change-password.tsx` (default export was confusingly named `ChangePasswordPage` for what is a form) moved to `features/settings/components/ChangePasswordForm.tsx` and renamed to `ChangePasswordForm`; its 3 importers (AuthScreen, app/(auth)/forgot-password/page.tsx, EditPasswordScreen) were updated, and the now-empty `features/settings/screens/` directory is gone. `PartiesScreen.tsx` no longer declares its own local `Profile` type and no longer queries `profiles` inline — it imports `getMyProfile`/`Profile` from `features/profile/services/profile.service`, so profile reads go through the service layer everywhere. A shared `getOrigin()` (SSR-safe, returns '' on the server) now lives in `lib/utils.ts`; it replaced the private copy in `auth.service.ts` and, more importantly, replaced the `origin` state + `useEffect(() => setOrigin(window.location.origin))` pattern in both `CreateEventScreen.tsx` and `EventDetailScreen.tsx` — that pattern was React 19's only two remaining `react-hooks/set-state-in-effect` lint ERRORS, and both are now gone. `types/database.types.ts` is generated by `supabase gen types typescript`, so it was added to eslint's `globalIgnores` instead of being hand-fixed (it alone accounted for 73 of the 77 quote errors, which would have come straight back on the next regeneration).
EventDetailScreen pass 2 — host dropdown, skeletons, browser-native errors, animations (all four verified: `tsc` clean, eslint 0 errors, route returns 200, SSR HTML contains 34 skeleton nodes, and the emitted CSS was inspected to confirm `.skeleton`, `@keyframes skeleton-shimmer`, `.grid-rows-[0fr]/[1fr]`, `.transition-[grid-template-rows]` and `.right-13.75` all compile).
(a) HOST DROPDOWN: the ••• button (`MoreIcon`) is now rendered for BOTH roles at `absolute right-0`, so the menu anchor is identical for host and guest; the host's copy-link button moved from `right-0` to `right-13.75` (45px button + 10px gap) and now sits to the LEFT of the •••. One `menuPanelClass` panel serves both roles: guests get the 3 `RSVP_MENU` rows + divider + a "Löschen" row wired to `handleLeaveEvent` (deletes only their own rsvp row); hosts get ONLY the "Löschen" row, wired to the new `handleDeleteEvent` → `confirm('Event wirklich löschen? …')` → `deleteEvent(event.id)` → `/parties`. This re-fills the host-delete gap the previous cleanup pass documented, so `deleteEvent()` in events.service.ts is live again.
(b) RSVP CHECKMARK: a lucide `Check` icon (`CheckIcon`, `ml-auto`) sits permanently on whichever RSVP row matches the current `rsvpStatus`, on the RIGHT of the row — the `bg-tertiary` pill highlight stays too. The apple emojis (✅🤔❌) remain the left column and are deliberately NOT lucide icons; lucide is used for every non-RSVP icon. Tapping a row no longer closes the menu instantly: `handleRsvp` awaits the write, moves the ✓, then `setTimeout(() => setMenuOpen(false), 600)` so the confirmation is visible. `handleRsvp` also stopped re-fetching the session (it uses the already-loaded `userId`), removing one round-trip per vote.
(c) SKELETONS: the single all-or-nothing `Promise.all` + `loading` flag (which rendered `null` for the whole page) was split into FOUR independent flags — `eventLoading` (event+host+rsvp status), `countsLoading`, `poolsLoading`, `attendeesLoading` — each clearing its own block's shimmer as its query lands, so the title appears while the map/polls/guests are still loading. The effect now has a `cancelled` cleanup flag. Skeleton style is a gradient sweep, NOT an opacity pulse: new globals.css tokens `--color-skeleton` (rgba(118,118,128,0.25), same value as `--color-secondary`) and `--color-skeleton-sheen` (rgba(255,255,255,0.08)), a top-level `@keyframes skeleton-shimmer` (translateX -100%→100%), and a `.skeleton` utility using a `::after` overlay with a `linear-gradient(90deg, transparent, sheen, transparent)` on a 1.5s infinite loop, plus a `prefers-reduced-motion` guard that kills the animation. Usage is class-based (`className='… skeleton'`) — there is deliberately NO Skeleton component wrapper. The hero image gets its own `heroLoaded` state: shimmer until the `<img onLoad>` fires, then the image cross-fades in over 500ms. The ••• spot holds a skeleton circle while `eventLoading`, because the host/guest role isn't known yet. Umfragen and Gäste DO show skeletons before their data lands and disappear entirely if they turn out empty (accepted layout shift). The shimmer is hand-rolled CSS on purpose — `tw-animate-css` was evaluated for this and could not do a gradient sweep, so it was uninstalled (`npm uninstall tw-animate-css`) rather than left as a dead dependency; do not re-add it. EventMap owns its own skeleton too — see the EventMap rewrite note below for how it ended up. InviteScreen shares EventMap, so it gets the same map skeleton.
(d) ERRORS: new module-level `alertError(message, detail?)` helper builds `alert('German sentence\n\nraw supabase detail')` — browser-native dialogs only, no custom UI, per explicit instruction. Wired into event-load failure, RSVP save, guest leave and host delete.
(e) COLLAPSE ANIMATION: the three Location/Umfragen/Gäste blocks now render through one local `Section({ title, open, onToggle, children })` component defined in EventDetailScreen.tsx (used 6x — 3 real + 3 skeleton variants — so it is not a single-use abstraction). Content animates height via the `grid-rows-[0fr]` → `grid-rows-[1fr]` trick with `transition-[grid-template-rows] duration-200` and an `overflow-hidden` inner div, which reaches the content's natural height without measuring it; the chevron keeps its existing `rotate-90 duration-200`. The section container lost its `gap-4` (a gap would still render when collapsed) — the inner wrapper carries `pt-4` instead, and the original per-section bottom spacing is preserved exactly (Location and Umfragen children carry `pb-7.5`, Gäste carries none, matching the old `mb-7.5` placement).
(f) DROPDOWN ANIMATION: the menu panel is now always mounted (so it animates on close too, not just open) and toggles `opacity-100 scale-100` ↔ `pointer-events-none opacity-0 scale-95` with `origin-top-right transition-all duration-150 ease-out`, so it grows out of the ••• button. `aria-hidden={!menuOpen}` and `aria-expanded` on the trigger; the click-away backdrop still only mounts while open.
(g) POOLSSECTION is now purely presentational — props are `{ pools, userId, onRefresh }`. It no longer fetches, which kills a real double-fetch (EventDetailScreen called `getEventPools` for its length check AND PoolsSection fetched the same rows again). Its `isHost` prop and its empty-state box are gone; that box was already invisible since it used the deleted `border-border`/`bg-background-secondary`/`text-hint` tokens. InviteScreen was updated to match: it now fetches pools in its own `Promise.all` and renders `<PoolsSection>` only when `pools.length > 0`.
Poll voting is now OPTIMISTIC. The old PoolCard drove the radio circle from local `selectedOptionId` state but drew the voter avatar stack from `pool.responses` (server data), so tapping an option moved the radio instantly while your own profile picture only jumped to the new option after the write + `onRefresh()` refetch had round-tripped — a visible multi-second lag. PoolCard now reconstructs its own row locally: `pendingOptionId` state holds the tapped option, `selectedOptionId = pendingOptionId ?? serverResponse?.option_id ?? null`, and a synthetic `myRow: PoolResponse` is merged into the response list — replaced IN PLACE via `.map()` when a server row already exists (so changing your vote never reorders the other voters) or appended when it's your first vote. Avatar fields prefer `myProfile` and fall back to `serverResponse`, so nothing regresses to '?' initials during the window before the profile fetch resolves. `onRefresh()` still runs after a successful write to reconcile with the server, but it is now invisible — the merged output is identical, so there is no flicker. On failure `pendingOptionId` rolls back to its previous value and `alertError` fires; previously `upsertPoolResponse` errors were swallowed entirely with no rollback and no message. This required threading a `myProfile: Profile | null` prop down EventDetailScreen → PoolsSection → PoolCard (EventDetailScreen and InviteScreen both now call `getMyProfile`; EventDetailScreen does it in its parallel load block). `alertError` moved from a module-level function inside EventDetailScreen.tsx to `lib/utils.ts` so PoolCard could share it. Merge logic was verified against 6 scenarios (first vote, post-refetch idempotence, switching options, error rollback, never-voted, unique React keys) — all pass.
EventMap was rewritten from a Google Maps EMBED IFRAME to a Google Maps STATIC IMAGE, because the iframe made a correct skeleton impossible. Measured directly: the embed URL returns only 2170 bytes in ~0.37s — a JS bootstrap with 6 script tags, not the map — so the iframe's `onLoad` fired almost immediately, the skeleton cleared, and the user then stared at a blank box for several seconds while Google's JS fetched and painted tiles. A cross-origin iframe gives no signal for "tiles painted", so the fix was to stop using one. The component now renders an `<img>` pointing at `maps.googleapis.com/maps/api/staticmap` (`zoom=15`, `size=640x246&scale=2` — 2.6:1, matching the `h-33` container's aspect on a 375px phone so `object-cover` barely crops; marker in brand pink `color:0xFF0090`), whose `onLoad` only fires once the PNG is decoded, so the skeleton now ends exactly when the map becomes visible. The whole block is wrapped in an `<a target='_blank' rel='noopener noreferrer'>` to `https://www.google.com/maps/search/?api=1&query=<location>` (the official Google Maps URLs API, which deep-links into the native app on mobile), so tapping the map opens it in Google Maps — this replaces the pan/zoom that was lost with the iframe. There is an `onError` → `failed` state that swaps in a tappable `bg-secondary` box reading "In Google Maps öffnen", so a missing key or a disabled API can never leave a skeleton shimmering forever.
Dependency cleanup: `radix-ui`, `shadcn` and `class-variance-authority` were removed (`npm uninstall`, −328 packages) — all three had ZERO imports anywhere, no `components.json` and no `components/ui` folder ever existed, so shadcn had never scaffolded anything. This follows the same reasoning that removed `tw-animate-css` earlier. Runtime deps are now only: @supabase/ssr, @supabase/supabase-js, clsx, lucide-react, next, react, react-dom, tailwind-merge. If shadcn components are ever wanted, re-add `shadcn` as a devDependency (it is a CLI, not a runtime library).
Git identity is configured globally as `Arthur Kunz <251507651+ArthurKunz@users.noreply.github.com>` (GitHub noreply, so the real address stays out of public history). Commits made before that was set carry `arthurkunz@MacBook-Pro-by-Arthur.local` and were deliberately NOT rewritten — no force-push.
Google Cloud setup for this (done, but note it if the key is ever rotated or replaced): the Maps Static API and the Maps Embed API are separate products, so BOTH steps were needed — (1) enabling "Maps Static API" on the project via the API library, and (2) adding Maps Static API to the `Maps-API-key` credential's API restrictions, which had been locked to Maps Embed API only. The two failures give different 403 texts and are easy to confuse: "This API is not activated on your API project" = step 1 missing, "This API key is not authorized to use this service or API" = step 2 missing. Verified working: the production URL returns HTTP 200, `image/png`, 1280x492, ~119KB in 0.6s, with the brand-pink marker rendering correctly. That key's Application restrictions are still "None" — worth tightening to Websites later, but it was left alone here to avoid breaking local dev.
EventDetailScreen's root div was `bg-white` (a leftover that could flash white below the content on a short event, in an otherwise all-black app) and is now `bg-main`, matching the content section it wraps.
The host is now always part of the guest list on the event detail page and the public invite page. Fixed in SQL, not on the client (migration `20260803120000_include_host_in_event_attendees.sql`): `get_event_attendees` now UNION ALLs the host's profile row (as `'going'`) onto the rsvp rows, guarded by a `NOT EXISTS` on the host's own rsvp so a host who has RSVPd appears exactly once with their real status; the whole thing is wrapped in a CTE so the going/maybe/not_going + firstname ordering still applies. Doing it in the function means both `EventDetailScreen` and `InviteScreen` (the only two `getEventAttendees` callers) got it for free — no TS changed, and the `attendees.length > 0` guard on EventDetailScreen's "Gäste" section now always passes, so an event with zero RSVPs shows the host instead of hiding the section. Verified against live data: the zero-rsvp event returns exactly the host; across every event there are no duplicate `user_id`s and the host is present in all of them; the dedupe branch was dry-run with a simulated host rsvp (returns one row, host's own status wins).
The two RSVP count functions follow the same rule now (migration `20260803130000_include_host_in_rsvp_counts.sql`), so the detail screen's "zugesagt" number can't disagree with the guest list below it: `get_rsvp_counts_by_status` adds 1 to `going_count`, and `get_rsvp_count` (going+maybe, used for `attendee_count`) adds 1, each guarded by the same `NOT EXISTS` on the host's own rsvp row — a host who has RSVPd is counted once under whatever status they picked, never double. Verified live: `going_count` now equals the number of `'going'` rows `get_event_attendees` returns, on both a zero-rsvp event (1/1) and a 2-rsvp event (3/3).
Consequence for `events.service.ts`: the client-side host prepending in `getHostedEvents` and `getAttendedEvents` was compensation for the DB not including the host, and is now removed — it would have DUPLICATED the host in `getHostedEvents` (which prepended unconditionally with `attendee_count + 1`); `getAttendedEvents` had a `!attendees.some(...)` guard so it degraded to dead code instead. `getHostedEvents` no longer fetches the host's own `profiles` row at all (it is back to a single query), and `getAttendedEvents`'s host row now carries the host's real `birthday` from the RPC instead of the hardcoded `null` the synthetic row used. `hostColor()` is still used for `host_avatar_color`'s fallback. Note this is currently invisible in the UI either way — the rewritten EventCard renders neither `attendee_count` nor `attendees` — but the data is correct now.
InviteScreen (`/e/[invite_code]`) was rewritten to match EventDetailScreen — it was the last screen still written against the pre-rewrite design system and had been skipped by every redesign pass. It had 21 dead token classes (`text-headline` ×7 on the title/countdown/counts, `text-hint` ×6, `border-border`+`bg-background-secondary` ×8, `bg-background-main`, `border-glass`/`bg-glass` on the old back pill), so the title, the counts and the description were all rendering colorless. It now shares EventDetailScreen's exact structure: `h-100` hero with the `background_url` image + `onLoad` cross-fade + the `from-main` gradient blend, the four independent skeleton flags (`eventLoading`/`countsLoading`/`poolsLoading`/`attendeesLoading`) replacing the old `if (loading) return null` blank screen, the horizontal stats row (Datum/Uhrzeit/Max. Gäste/zugesagt/abgesagt/vielleicht), the address + "in {city}" split, and the collapsible Location/Umfragen/Gäste sections. Gone with it: the 90px countdown hero, the fixed blurred full-page background, the boxed date/time tiles, the 3 count boxes, the "mehr lesen" description truncation, and the local `getCountdown()`.
The collapsible `Section` component was extracted from EventDetailScreen.tsx into `features/events/components/Section.tsx` (with the `ChevronRightIcon` it owns) since both screens now render it — EventDetailScreen imports it and lost its local copy plus its now-unused `ChevronRight` and `ReactNode` imports. The rest (icon consts, `iconButtonClass`, `menuPanelClass`, `RSVP_MENU`) is deliberately duplicated rather than shared, because the two menus differ in behavior.
InviteScreen's header is EMPTY while the page loads — no back button and no skeleton placeholder circle — because until the event and session resolve we do not know whether the viewer is the host, a guest or someone without an account. One `showHeaderControls = !eventLoading && !showAuthGate && !showRsvpGate` flag drives all three controls (back, copy-link, •••), so they appear together the moment the role is known and stay hidden entirely behind either gate. Note EventDetailScreen still renders its back button immediately plus a skeleton circle for the •••, which is fine there — that route already requires a session, so only host-vs-guest is unknown.
InviteScreen branches host vs guest on load, exactly like EventDetailScreen: `isHost` is set from `eventData.host_id === uid` in the load effect. A HOST opening their own invite link gets the full host overview — the copy-link button at `right-13.75` plus a ••• menu whose single row is "Löschen" → `handleDeleteEvent` (`confirm(...)` → `deleteEvent(event.id)` → `/parties`), the same handler the detail page uses. A non-host gets guest rights unchanged (the 3 RSVP rows). The top-left back button always routes to `/parties` now — it used to be `window.history.length > 1 ? router.back() : router.push('/parties')`, which sent people back to whatever external page the link came from.
InviteScreen's ••• menu differs from the detail page's in two deliberate ways: (1) anonymous visitors get the 3 RSVP rows but tapping any of them routes to `/login` instead of writing, (2) the guest's "Löschen" (leave) row only renders once `rsvpStatus !== null` — on an invite link most viewers have not answered yet, so there would be nothing to delete (the host's Löschen row always renders, since deleting the event does not depend on an rsvp). Anonymous visitors also get a full-width `bg-tertiary` "Anmelden & zusagen" button at the bottom of the content, because replacing the old always-visible `RsvpButtons` row with a ••• menu would otherwise have hidden the primary call to action from exactly the person the page exists for. Umfragen stay hidden for anonymous visitors (unchanged — PoolCard needs a userId to vote).
`RsvpButtons.tsx` and `HostRow.tsx` were deleted — InviteScreen was their only importer, and this rework replaced both (the RSVP row by the ••• menu, the host row by the inline avatar+name block copied from EventDetailScreen).
Signing up or logging in from an invite link now RETURNS to that invite link instead of dumping the user on `/home`. A `?next=` query param is threaded through the whole funnel: InviteScreen's sheet buttons build `loginHref = /login?next=/e/{inviteCode}` → `AuthScreen` reads it and uses it for the sign-in exit AND appends it to the onboarding URL it pushes after OTP verification → `OnboardingScreen` reads `?next=` and pushes there after inserting the profiles row (instead of its hardcoded `/home`) → `app/(auth)/callback/route.ts` reads it too, so the OAuth round-trip lands in the same place once Google is wired up. Every read goes through `sanitizeNextPath()` in `lib/utils.ts`, which accepts only same-site paths (must start with `/`, must not start with `//`) — without it, `?next=` would make the login flow an open redirector. `app/onboarding/page.tsx` gained a `<Suspense>` wrapper because `OnboardingScreen` now calls `useSearchParams()`. Verified: `/callback` redirects to `/home` with no param, to `/e/dede5343` with a valid one, and falls back to `/home` for both `https://evil.example` and the protocol-relative `//evil.example`.
Deliberately NOT done (decided with Arthur): the auth forms stay on `/login` rather than moving into the sheet, because only ONE of the three paths could actually stay on the page — email login is a same-tab API call, but email signup still has to leave for the 3-step onboarding that inserts the profiles row, and Google's `signInWithOAuth` unloads the page by definition. Returning from auth also does NOT auto-RSVP or auto-open the ••• menu; the user lands on the plain event page and answers themselves. Note `signInWithGoogle`/`signUpWithGoogle` in `auth.service.ts` still pass `redirectTo: getOrigin()` (not `/callback?next=…`), so they will need updating when the provider buttons are actually wired up. Also note CLAUDE.md's older claim that signed-in users on an invite link redirect to `/parties/[id]` is STALE — there is no such redirect, signed-in users render InviteScreen itself (which is now equivalent to the detail page anyway).
Verified: `tsc` clean, eslint 0 errors / 15 warnings (unchanged count — HostRow's `<img>` warning moved into InviteScreen), `/e/dede5343` returns 200 with 30 skeleton nodes and 0 dead tokens in the SSR HTML, `/parties/[id]` still returns 200 with its 34 skeletons after the Section extraction, and the dev server log is free of runtime errors.
You can no longer be a guest at your own party. An event was showing on BOTH PartiesScreen tabs at once ("Gastgeber" and "Gast") because the host had an rsvp row on their own event — the old InviteScreen rendered `RsvpButtons` to everyone, host included, so a host who opened their own invite link could RSVP to themselves. Fixed at two levels: (1) migration `20260803140000_forbid_host_rsvp_to_own_event.sql` rewrites the `rsvps_insert_authenticated` and `rsvps_update_own` RLS policies to add `AND NOT EXISTS (SELECT 1 FROM events e WHERE e.id = event_id AND e.host_id = auth.uid())` to their `WITH CHECK`, so the database itself rejects a host's rsvp on their own event; (2) `getAttendedEvents` now filters out `r.events.host_id === userId`, so even a pre-existing row can never surface the event under "Gast". Note this is belt-and-braces on top of the UI, which already hides RSVP controls from hosts on both EventDetailScreen and the reworked InviteScreen. Verified: both policies are live with the new `WITH CHECK`, and the predicate evaluates to false for a host on their own event / true for a guest on someone else's. One pre-existing offending row (Mark's 'going' on his own Gartenparty) still exists in the database — it is now invisible in the UI and harmless to the counts, but it is the row the new policy would reject.
Two console errors seen while testing on a phone over ngrok, investigated and NOT both bugs in this codebase. (1) "A tree hydrated but some attributes of the server rendered HTML didn't match" — CONFIRMED as Google's translate/remote-frame injection, not a bug here. `.next/dev/logs/next-development.log` captured the actual diff, and it is a single injected attribute on the root element: `<html lang="de" __gcrremoteframetoken="f53ad47f5df82aaa0f4168f5e65fd353">`. That attribute is written by Google (the Translate bar is visible in the screenshots), which is why the fix belongs on `<html>`. The codebase itself has no render-path nondeterminism (checked: `Date.now()`/`crypto.randomUUID()` only inside event handlers, `new Date()` only in DateTimePicker's create-flow render, no `window`/`localStorage` reads during render, no nested `<a>`/invalid nesting in PartiesScreen/EventCard/BottomNav). Mitigated with `suppressHydrationWarning` on `<html>` and `<body>` in `app/layout.tsx`, which is the standard fix for translator/extension attribute injection — note it only suppresses ONE level, so a genuine mismatch deeper in the tree would still be reported. (2) `AuthApiError: Invalid Refresh Token: Refresh Token Not Found` needs NO code change: `@supabase/auth-js` 2.104.1 prints it itself at `GoTrueClient.js:3838` (`console.error(error)` inside `_recoverAndRefresh`) and then calls `_removeSession()` on the next line because the error is non-retryable, so the dead token is already cleared and the app's own `if (!session) router.push('/login')` guards take over. No error handling added at the 11 `getSession()` call sites would silence a log the library emits internally. It fires when the stored refresh token was already rotated or revoked — typical when the same account is open on a phone and a laptop at once, which is exactly the ngrok test setup.
`app/layout.tsx`'s `themeColor` was `'#09090B'` (pre-rewrite near-black) while `--color-main` is pure `#000`, so Safari's top and bottom bars framed the app in a slightly lighter black; it is now `'#000000'` and they blend in. (This is the only way to color those bars — they are browser chrome, not part of the page.)
InviteScreen now hard-gates anonymous visitors behind a sign-up sheet (built from a Figma-style mockup). Once `!eventLoading && !userId` (`showAuthGate`), the root div gets `h-dvh overflow-hidden`, a `fixed inset-0 z-30 bg-main/30 backdrop-blur-xl` layer blurs the whole event page behind it, and a `fixed inset-x-0 bottom-0 z-40 rounded-t-[2.5rem] bg-sheet` panel holds the gate: an empty dark placeholder circle (a logo goes there later), the headline "Sign up oder login", a two-sentence explanation, then "Erstelle ein Account" (black) and "Bei Account Anmelden" (light grey) — BOTH currently route to `/login`, which opens the signup form, because fixing the sign-in entry point was explicitly deferred — and a row of two `disabled` provider buttons, Apple LEFT and Google RIGHT, whose logos are inline SVG paths since lucide dropped brand marks. Those two are intentionally non-functional until the OAuth providers are added. This replaced the earlier "Anmelden & zusagen" button. The blur layer sits above the hero's ••• menu (z-20), so an anonymous visitor cannot open it at all; the `if (!userId) router.push('/login')` guard at the top of `handleRsvp` is now unreachable in practice but kept as a cheap safety net.
Scroll lock took three pieces, because clipping the page container was NOT enough on its own — `h-dvh overflow-hidden` on the root div bounds and clips the content, but the DOCUMENT stays scrollable and mobile browsers happily scroll it right through a `fixed` overlay. Added: a `useEffect` keyed on `showAuthGate` that sets `overflow: hidden` on both `document.body` and `document.documentElement` (restoring the previous values in its cleanup, so leaving the gate never leaves the app unscrollable), plus `touch-none overscroll-none` on the blur layer so a touch-drag over the blur produces no pan gesture and no rubber-band pass-through. NOTE this forced `showAuthGate` to move ABOVE the `if (notFound) return` early return, next to the other hooks — a `useEffect` after a conditional return breaks the rules of hooks.
A SECOND gate sits on InviteScreen for signed-in guests who have not answered yet (`showRsvpGate = !eventLoading && !!userId && !isHost && rsvpStatus === null`): the back button and the ••• button are both removed from the header, and a fixed bar takes over the bottom of the page with the three RSVP controls from the mockup — a red circle (lucide `X`, `border-warning/60 bg-warning/15`) for absagen, a wide green pill (✅ + "zusagen", `border-success/60 bg-success/15 text-success`) and a yellow circle (🤔, `border-maybe/60 bg-maybe/15`) for vielleicht. All three are `h-12.5` (50px — note the project's spacing scale is 4px-based, so 50px is `12.5`, NOT `h-50`, which would be 200px); the circles are `w-12.5`, the pill is `flex-1 max-w-47.5 px-6` with `text-subheading-1` labels and a 22px X icon. All three call the existing `handleRsvp`, so answering flips `rsvpStatus`, the bar disappears and the header controls come back in the same render. No new color tokens were needed — the bar reuses `--color-success/maybe/warning` with opacity modifiers. The bar's container is fully transparent — each of the three buttons carries its own `backdrop-blur-md` instead, so content scrolling underneath is blurred only where a button actually sits (an earlier `from-main via-main to-transparent` gradient behind the whole bar was tried and removed). It sits on the new `.pb-safe-rsvp` utility (`calc(2rem + env(safe-area-inset-bottom))`, same pattern as `.pb-safe-nav`), and the content column switches from `pb-7.5` to `.pb-safe-rsvp-content` (`calc(5.5rem + env(safe-area-inset-bottom))`) while it is up, which is just enough to clear the bar's 50px buttons plus its own 2rem — an earlier flat `pb-40` (160px) left an obvious empty gap under the Gäste list. Note the left icon is lucide `X` rather than the ❌ emoji the ••• dropdown uses — the mockup clearly shows a stroked X, while ✅ and 🤔 are unmistakably emojis there, so this one row mixes the two on purpose. Also note this gate is InviteScreen-only; EventDetailScreen keeps its back button and ••• for a guest with no rsvp, since hiding the back button there would strand someone who arrived from /parties.
This is the app's first light surface, so five tokens were added to globals.css with permission: `--color-sheet` (#FFFFFF), `--color-sheet-heading` (#0A0A0A), `--color-sheet-body` (#8A8A8E), `--color-button-primary` (#000000) and `--color-button-secondary` (#E9E9EB). Verified they compile — all five appear in the emitted stylesheet with the right values (`--color-sheet: #fff`, `--color-button-secondary: #e9e9eb`, …). Note the gate only renders after the session check resolves client-side, so it is absent from the SSR HTML by design (otherwise it would flash over the skeletons on every load) and cannot be verified with curl — it needs a real browser with no session.
The event description on EventDetailScreen and InviteScreen is now clamped to 3 lines and expandable, via a new shared `features/events/components/EventDescription.tsx` (both screens previously rendered the same bare `<span className='text-body text-body-1'>{event?.description}</span>`, now both render `{event?.description && <EventDescription text={event.description} />}`). Collapsed it is a `line-clamp-3` `<p>`; whether it actually overflows is measured once in a `useEffect` keyed on `text` (`el.scrollHeight > el.clientHeight + 1`, only meaningful while clamped), so the button never appears on a short description. When it does overflow, an absolutely-positioned `bottom-0 right-0` button sits on the third line carrying `… Mehr anzeigen` — it has a solid `bg-main` background (covering the browser's own line-clamp ellipsis) plus a `right-full w-8` left-to-right gradient sibling so the text fades into it instead of being cut off. Expanded, the clamp is dropped (`whitespace-pre-line` preserves the author's line breaks) and a `Weniger anzeigen` button renders below the text. Both labels use the new `--color-link: #0A84FF` token (added with permission — the only pre-existing blue, `--color-brand-blue` #161BFA, is near-unreadable on the black background); verified `--color-link`, `.text-link` and `.line-clamp-3` all appear in the emitted CSS. There is deliberately NO resize listener — the measurement runs on mount/text change only, so an orientation change can leave a stale button state until re-navigation.
Expanding/collapsing that description is ANIMATED (300ms ease-out, `motion-reduce:transition-none` on all three transitions). `line-clamp-3` only survives until the first measurement — after that the paragraph is a plain block and an `overflow-hidden` wrapper with an inline `max-height` does the clipping instead, so the exact same three lines show but the height is now an animatable px value. The same effect that sets `overflowing` also stores `collapsedHeight = el.clientHeight`; `fullHeight` is read lazily in the expand handler (`boxRef.current.scrollHeight`, reliable there because the clamp is already gone and layout has settled) rather than measured up front. The `… Mehr anzeigen` button is rendered OUTSIDE that animated wrapper — otherwise it would ride the growing box downwards — and is pinned to the third line with `style={{ top: collapsedHeight }}` plus `-translate-y-full`, which lifts it by its own single-line height so its bottom edge lands exactly on the bottom of line 3 with no magic pixel constant. It fades out over 200ms (`opacity-0 pointer-events-none`) instead of unmounting. `Weniger anzeigen` uses the `grid-rows-[0fr]` → `[1fr]` idiom copied from `Section.tsx` so it grows in sync with the text. Both buttons stay mounted while hidden, so both carry `aria-hidden` + `tabIndex={-1}` in their hidden state. Caveat, same class as the missing resize listener: while expanded the wrapper holds a fixed px `max-height`, so a viewport change that reflows the text taller would clip it until it is collapsed and re-expanded. Verified: eslint 0 errors, `tsc` clean, and `.transition-[max-height]`, `.-translate-y-full`, `.grid-rows-[0fr]/[1fr]` and `.text-link` are all present in the emitted stylesheet.
Remaining 14 eslint warnings are all intentional and were left alone: 12x `@next/next/no-img-element` (real `<img>` tags for Supabase-storage avatars/backgrounds — switching to `next/image` needs `remotePatterns` configured in next.config.ts first) and 1x `react-hooks/exhaustive-deps` in DateTimePicker.tsx. BUILD IS HEALTHY — do not go hunting for a `/_global-error` bug. `next build` fails with `TypeError: Cannot read properties of null (reading 'useContext')` while prerendering Next's internal `/_global-error` page ONLY when `NODE_ENV=development` is present in the shell (Next warns `non-standard "NODE_ENV" value` on the very first line when this happens — that warning is the tell). Next expects `NODE_ENV=production` during a build, and the mismatch makes React resolve to a build whose dispatcher is null. With the variable unset, `env -u NODE_ENV npx next build` exits 0 and prerenders all 16 pages / 17 routes. Real deploys (Vercel etc.) set `NODE_ENV=production` themselves, so this never affected them. Also noted, not acted on: CLAUDE.md claims `middleware.ts` is in place, but no `middleware.ts` and no `lib/supabase/server.ts` exist in the repo — every screen guards its own session client-side via `supabase.auth.getSession()` instead.
The header icon buttons on the hero (back, copy-link, •••) are now `bg-main/50` instead of `bg-secondary` — `iconButtonClass` in both `EventDetailScreen.tsx` and `InviteScreen.tsx` (the two copies are still deliberately duplicated, but they must stay identical). Combined with the existing `backdrop-blur-xs` this reads as a darkened blur over the hero image rather than the flat grey circle used elsewhere in the app; the create-event '+' button and the other `bg-secondary` circles are unchanged.
The ••• dropdown no longer opens BELOW the button — the button IS the dropdown now, on both screens. `menuPanelClass` (which was `absolute right-0 top-13 … w-56.25 bg-quaternary/50` + an opacity/scale fade) is replaced by `menuContainerClass` = `absolute right-0 top-0 z-20 min-h-11.25 overflow-hidden bg-main/50 backdrop-blur-xs transition-all duration-300 ease-out`, i.e. the exact same surface as `iconButtonClass`, anchored at the buttons' own top edge. Open it toggles `w-11.25` → `w-56.25`, so it grows leftwards from the top-right corner, and the height is an inline px value (`style={{ height: menuOpen ? menuHeight : MENU_CLOSED_SIZE }}`, `MENU_CLOSED_SIZE = 45`) measured lazily in `openMenu()` from `menuContentRef.current.scrollHeight`. TWO smoothness fixes are baked into that and must not be undone: (1) the radius is CONSTANT `rounded-[22.5px]` — exactly half the closed size, so the closed box is still a true circle — because animating `rounded-full` → `rounded-3xl` reads as a POP: 9999px→24px only stops looking like a semicircle once the value drops under half the box height, which is the last few frames. (2) The height is an explicit px value rather than the `min-h-11.25` + `grid-rows-[0fr]→[1fr]` idiom the first version used (and that `Section.tsx`/`EventDescription.tsx` still use, correctly, since they have no minimum) — with a min-height the first 45px of the content's growth is swallowed and the box visibly sits still before it starts moving. Easing is `ease-[cubic-bezier(0.32,0.72,0,1)]` over `duration-300` on `transition-[width,height]`. Re-measuring is not needed while open: the rows are static on EventDetailScreen, and on InviteScreen a guest whose `rsvpStatus` is null is behind `showRsvpGate`, so the menu is unreachable in the one state where a row would appear. The ••• trigger is an `absolute inset-0` button inside that container, fading out over 150ms as the panel opens; the row content fades in with `delay-150` so it only appears once the box has room, and its wrapper is a fixed `w-56.25` so labels never reflow while the box is still narrow. The host's copy-link button fades out the same way (the panel expands right over its spot), so while the menu is open BOTH header buttons are gone — the only way to close it is the `fixed inset-0` click-away backdrop (or an action row, unchanged: RSVP closes after 600ms, Löschen navigates). Trigger and copy button carry `aria-hidden`/`tabIndex={-1}` while open, matching the pattern EventDescription uses for its hidden-but-mounted buttons. PartiesScreen was brought in line with the detail page (skeletons, lucide icons, motion). Its root is `bg-main` — it was still on the dead `bg-background-main` token, which rendered no background at all and only looked right because `html` is `#000`. The header's hand-rolled two-line `+` SVG is now lucide `Plus` (`PlusIcon`, size 24 strokeWidth 3); the header buttons share one `iconButtonClass` = `h-11.25 w-11.25 bg-secondary rounded-full … transition-transform duration-200 ease-[cubic-bezier(0.32,0.72,0,1)] active:scale-95`, i.e. the detail page's 45px circle and easing but KEEPING `bg-secondary` — decided with Arthur, because the detail page's `bg-main/50` only reads as grey thanks to the hero photo behind it, and on this page's black background those circles would be invisible. The empty state's hand-drawn doodle arrow deliberately stays a custom SVG (it has to curve down onto the bottom nav's `+`; lucide has no equivalent), also decided with Arthur. Loading is no longer `loading ? null` (a blank page): a local `CardSkeleton({ featured })` component (used 5x) mirrors the real card — `aspect-[2/1]` or `aspect-square` shimmer block plus two text bars — rendered as one featured card + a 4-item 2-col grid, and the profile avatar has its own `profileLoading` flag so it clears independently, matching the detail page's per-block flags (`getMyProfile` moved out of the `Promise.all` for that). EventCard now does the hero's image treatment: `imgLoaded` state, `.skeleton` overlay until `<img onLoad>` fires, then a 500ms opacity cross-fade; its press feedback went from `active:scale-[0.98]` (untimed) to `active:scale-[0.97]` with the same 200ms cubic-bezier as everything else. The Gast/Gastgeber tabs switched from a background class jumping between the two buttons to ONE absolutely-positioned pill (`top-1 bottom-1 left-1 w-[calc(50%-0.25rem)]`, which is exactly one tab's width, so `translate-x-full` lands it precisely on the second tab) sliding over `duration-300` with the same easing; the buttons sit above it at `relative z-10`. The list block carries `key={loading ? 'loading' : tab}` + a new `.animate-fade-in-up` utility (globals.css: `@keyframes fade-in-up`, opacity 0→1 with an 8px rise, 0.3s, same cubic-bezier, `prefers-reduced-motion` guard next to `.skeleton`'s), so it replays its enter animation both when the real cards replace the skeletons and on every tab switch. PartiesScreen has a decorative background layer: `features/events/components/FloatingEmojis.tsx` renders bare party emojis at 40–60px (the sizes Arthur specified — they started as white `bg-sheet` circles with the emoji at 55% inside, and the circles were dropped: the emoji itself now carries the size) (34 of them, from two reference images Arthur sent: 🎈🎉🥳🎂🎁🍾🥂🪩✨🎵🎶🎤💃🕺 plus 😁🤣😂😍😜🤪😲😉👥🥃🍺🍷🍻🍸🎸🎫🎟️❤️🔞🔊), drifting bottom-to-top and off the screen forever. The component also renders a SECOND fixed layer on top of the emojis — `pointer-events-none fixed inset-0 z-[1] backdrop-blur-xs`, no background color at all — which is the "overlay" Arthur asked for: because `backdrop-filter` only affects what is painted BELOW an element, that one veil softens every emoji while the page content above it (cards, header buttons, the Gast/Gastgeber pill, all of which carry their own `backdrop-blur-xl`) stays perfectly sharp. Layer order is therefore emojis `z-0` → veil `z-[1]` → content column `relative z-10` → BottomNav `z-30`. The emoji layer additionally carries an `EDGE_FADE` mask (`mask-image`/`-webkit-mask-image`, `linear-gradient(to bottom, transparent 0%, #000 14%, #000 84%, transparent 100%)`), so an emoji is invisible as it enters at the bottom and has faded out again before it leaves at the top — no popping at either screen edge. This is a MASK rather than the two black gradient divs Arthur first suggested, because masking makes the emojis themselves transparent, which also reads correctly where one passes behind a card; a black gradient laid on top would only work against the plain background. It is the BOTTOM layer by design (`pointer-events-none fixed inset-0 z-0 overflow-hidden`, under the content column's existing `relative z-10`), so the translucent cards show them through — Arthur's mockup has them blurred behind the cards, and that blur pass is a SEPARATE follow-up, not done here. It is a SPAWNER, not a fixed set of looping lanes (that was the first version): it takes an `active` prop (PartiesScreen passes `!loading`) and spawns NOTHING until the page's data has landed and the skeletons are gone — so the screen starts completely empty and fills from the bottom as the page settles, which is the effect Arthur wanted; once active, a 5000ms `setInterval` starts ONE new emoji every five seconds (1/s and 1/2s were both tried on a phone and were far too dense — the whole screen filled with white circles), each with its own random emoji, `left` (−2%…96%), size (40–60px) and flight time (20–35s). Movement is CSS — `@keyframes float-up` (`translate3d(0, 115dvh, 0)` → `-25dvh`, so each one starts fully below the fold and exits fully above it) plus an `.animate-float-up` utility that sets only name/linear/**iteration-count 1**/fill-mode forwards; duration and delay come from each element's inline style. Iterating once and holding the end position is deliberate: the element is removed a moment later by the spawner, and an infinite loop would visibly drop it back to the bottom in the gap. Three details that keep it honest: (1) state starts EMPTY so the server renders nothing — everything here is `Math.random()`, and building it during render would be a hydration mismatch, which this app has already been bitten by; (2) seeding is now a PROP rather than a global choice — an early version seeded ~6 emojis mid-flight (negative `animationDelay`) so the page looked full immediately, and that was removed because on the parties page it meant emojis hanging on screen behind the skeletons. It came back for the pages that have no skeletons: `FloatingEmojis` takes `seed`, and with it the first frame lays down `SEED_COUNT` (5) floaters that have already flown part of their path instead of one at the bottom. `makeSeededFloater` gives each an `offset` in seconds — rendered as a negative `animationDelay`, with `expires` shortened by the same amount so the cleanup filter still drops them on time — spread evenly across `SEED_FROM` 0.15 to `SEED_TO` 0.8 of the flight, which is exactly the band where a floater is fully on screen (the path runs 115dvh → −25dvh and the layer's mask fades both edges); the five land at roughly 85, 67, 49, 30 and 12dvh. `seed` is passed ONLY by `AuthScreen` and `OnboardingScreen`, the two screens whose content is a sheet with nothing to load — Arthur asked for it there because the fill-from-empty was just a wait. `PartiesScreen` (and `ProfileScreen`, which also gates on `active={!loading}`) must NOT get it, for the original reason. Hydration is unaffected: the state still starts EMPTY, so the server renders no floaters at all — verified, `/login` and `/onboarding` SSR contain zero `animate-float-up` nodes. The first spawn fires from a `requestAnimationFrame` callback because a synchronous `setState` in an effect body is a react-hooks lint ERROR in this repo; (3) each floater carries an `expires` timestamp and every spawn tick filters out the dead ones, so the DOM settles at a handful of nodes instead of growing without bound. `prefers-reduced-motion` hides the layer entirely (`display: none`) rather than freezing it, since a frozen emoji would sit stacked at the top edge. The layer is `fixed`, so it behaves like a background while the event list scrolls over it.
The hand-drawn doodle arrow is GONE (it was the thing pointing at the bottom nav's `+`) — the "Gastgeber" empty state now ends in a white pill button, `Party erstellen` with a lucide `Plus` to the RIGHT of the label, linking to `/create-event`. It reuses the sheet pair (`bg-sheet` / `text-sheet-heading`) that the invite page's auth gate introduced, so it needed no new tokens, and carries the same `active:scale-95` + cubic-bezier press as the header circles. The "Gast" tab now shows the SAME empty state as "Gastgeber" instead of a bare text line, so the two were pulled into one local `EmptyState({ title, text, actionLabel })` component (the card mockup and layout are identical; only the wording differs — Gastgeber: "Party schmeißen?" / "Party erstellen", Gast: "Noch keine Einladung?" / "Eigene Party starten"). BOTH buttons link to `/create-event`, because with no explore page in V1 that is the only thing a user with zero events can actually do; the old `emptyMessage` const is gone with the text line it fed. The empty state deliberately does NOT run `animate-fade-in-up` (a `showEmptyState` flag drops the class from the keyed wrapper): its mockup boxes use `backdrop-blur-xl`, and an animating ancestor — opacity OR transform — renders the whole subtree into its own compositing group, which has no page backdrop to sample, so on a phone the blur was visibly missing for the animation's full 300ms after every tab switch. There is no way to keep both; the blur won. Skeletons and event cards carry no `backdrop-filter`, so they still animate; the old `emptyMessage` const is gone with the text line it fed. Both empty states are vertically CENTRED in the area under the tabs, which took three pieces: `min-h-dvh` on the inner content column (the outer div already had it, but the column itself had no height for a child to grow into), `flex-1` on the keyed list block, and `flex-1 … justify-center` on the empty state itself. The populated list is unaffected — its children still stack from the top.
The "Gastgeber" empty state's card mockup was then scaled to 0.75 of its original size (box `h-36 w-64` → `h-27 w-48`, and every child value times 0.75 — `h-28 w-28` → `h-21 w-21`, `left-14 top-8 h-24 w-48` → `left-10.5 top-6 h-18 w-36`, the skeleton text bars from `h-2`/`gap-1.5` to `h-1.5`/`gap-1`, `rounded-2xl` → `rounded-xl`), so the proportions and the arrow's relationship to it are unchanged. Noted, not changed: `BottomNav.tsx` still uses the dead `bg-background-main` (harmless — `html` is black), as do the profile screens, `/home` and `/forgot-password`.
Verified in the emitted CSS: `rounded-[22.5px]` → `border-radius: 22.5px`, `transition-[width,height]` → `transition-property: width, height`, `ease-[cubic-bezier(0.32,0.72,0,1)]`, `w-56.25` → 225px and `delay-150` all compile (note Tailwind escapes the commas in those selectors, so grep for `transition-\[width\,height\]`).
CREATE-EVENT FLOW, second pass — walked step by step with Arthur and corrected each one. Step ORDER is now `name → date → time → location → background → description → guests → pools → done`: background moved up next to the other required answers, since it is the only later step that cannot be skipped. Everything derives its position from that one `STEPS` array, so reordering it was the whole change.
INPUT COLOURS are now one rule across the flow, and it came from the Name step: the typed value is `text-subheading` (light grey) and the placeholder is whatever the browser paints for `::placeholder`, which modern engines derive from `currentColor` at 54% — a much darker grey. The Datum and both Uhrzeit rows were `text-label-large` (white) when filled, and the description/poll `Details` textareas were white text with an explicit `placeholder:text-subheading`, i.e. exactly inverted. All of them now match. The date and time rows achieve it by construction rather than by copying values: the right-hand side is a real `<input>` carrying `rowInputClass`, `readOnly` + `tabIndex={-1}` + `pointer-events-none`, with the row button around it still owning the tap. Their placeholder is the word `auswählen` (an example date `7. März 2011` and example times were tried first and Arthur removed them); the picked date renders long-form via `toLocaleDateString('de-DE', { day: 'numeric', month: 'long', year: 'numeric' })`.
THE WHEEL commits its default on OPEN. `openDateSheet`/`openTimeSheet` write the value the sheet is about to display into state before showing it, so opening the sheet, changing nothing and closing keeps what you saw instead of falling back to the placeholder — previously the value only existed if `onChange` had fired, so you had to scroll one notch and back. `WheelSheet` rows are also `<button>`s now: `handleTap(i)` raises the existing `userScrolling` ref FIRST (otherwise the effect that syncs the list to `index` snaps the scroll instantly and kills the animation), smooth-scrolls that row under the band and calls `onChange(i)` directly so tapping the already-centred row still commits. That is the shared wheel, so BirthdayPicker gained tap-to-select too; it did NOT gain the commit-on-open, which lives in the create flow's own handlers.
LOCATION is one field now, not two. `AddressSearchSheet` was turned inside out into `features/events/components/AddressSearchField.tsx` and the sheet deleted (it had no other caller): a single pill with a lucide `Search`, no label, the placeholder `Adresse`, and a filled `bg-tertiary` circle holding a lucide `X` at the right end once there is a value. The whole pill is a `<label>` so tapping anywhere focuses the input. The `Stadt` row is gone, so `handleFinish` builds the stored string as `[address, city].filter(Boolean).join(', ')` — city only exists when a suggestion was picked, and the event pages already split at the LAST comma and show no `in {city}` line when there is none.
The geocoder is PHOTON (`photon.komoot.io/api/`), not Nominatim. Same OpenStreetMap data, still free and keyless, but built for type-ahead: Nominatim matches whole addresses, so `B` or a half-typed street returns nothing, and its usage policy asks not to be used for autocomplete at all. Minimum query length is therefore 1, not 3. Photon has NO country parameter, so the request is boxed to roughly DACH (`bbox=5.8,45.7,17.2,55.1`) and hits from across the borders are dropped client-side on `countrycode ∈ DE/AT/CH` — which is why it asks for `limit=15` and slices to 6 AFTER filtering. Results map from GeoJSON: street = `street + housenumber` falling back to `name` (a club, a park, a whole city keeps working), city = `city ?? state`, and the grey subtitle is name + address + postcode + city + country through a `Set`, because a plain house repeats the same string as both name and address. React keys use the OSM id — two hits legitimately share a label (Karl-Liebknecht-Straße appears twice in Berlin under different postcodes).
The suggestion list opens ABOVE the field and reads BOTTOM-UP: `[...results].reverse()` puts the best match in the row closest to what was typed, and an effect keyed on `[results, open, loading]` sets `scrollTop = scrollHeight` so the list opens at its bottom — the best match is whole and tappable right above the input, and it is the WORST one that gets cut off at the top edge. Reversing the array without that scroll was the first attempt and left the good result hanging off the bottom. The list is tied to focus (`open` state) and closes on blur via a 150ms timer that `handlePick` cancels — a tap on a suggestion blurs the input BEFORE the button fires its click, so an immediate close swallows the pick. Typing after a pick re-opens it, since `onChange` sets `open` itself.
`canContinue` for location is `locationPicked && location.trim().length > 0`: the answer must come from a TAPPED suggestion, and editing the text by hand clears both the flag and the city. Filtering the list down to exact addresses (`properties.type === 'house'`, which also passes named venues — the Distillery comes back as `house` with a housenumber) was built and then REVERTED at Arthur's request, along with its hint line; any suggestion counts now, including a bare street or a city. `properties.type` is still the field to use if that idea ever comes back.
`CreateStepLayout` was rebuilt around PAGE scrolling. It was `h-dvh overflow-hidden` with the answer `mt-auto`, so a step taller than the screen (backgrounds) was clipped and unreachable — you could not scroll, see the presets or press the button. First fix made the answer block an inner scroller (`min-h-0 overflow-y-auto`), which introduced a second bug worth remembering: with a very tall flex item present, flexbox takes the missing space out of every SHRINKABLE sibling, and the `h-12.5` button collapsed to its text height. The layout now scrolls the PAGE instead: root is `min-h-dvh` (no `overflow-hidden`), the ✕ / skip row is `fixed ... top-0 z-20`, the content column is `min-h-dvh ... pt-26.25` (30px inset + the 45px button + the 30px gap the headline always had) and keeps `mt-auto`, and the button + dots sit in a `fixed ... bottom-0` strip. BOTH strips are fully TRANSPARENT — no black bar; content passes behind them and the button's own `backdrop-blur-xl` blurs it, the same treatment as InviteScreen's RSVP bar. New `.pb-safe-step` utility in globals.css (`calc(8.5rem + env(safe-area-inset-bottom))`) keeps the content clear of that strip. The headline gained `mb-7.5`, because on a tall step `mt-auto` has no free space left to give and the headline was touching the answer; short steps are unaffected, `mt-auto` just absorbs 30px less.
A `pinnedControls` prop (default `true`) turns that bottom strip off for the two steps whose content grows — BACKGROUND and POOLS. With it false the button and dots render as the last children of the scrolling column instead, the column's padding drops to `pb-safe-rsvp`, and the button carries `mt-5.5`. That 22px is not arbitrary: it is exactly the gap a pinned step already has (`pb-safe-step` 136px minus the strip's own 114px = 50 button + 24 + 8 dots + 32 inset), so the button sits the same distance below the answer either way. Note 30px was used there briefly when Arthur asked for more room under the wallpapers, and matching the Name step later won.
`PoolDraftForm` has its own layout and was fixed the same way afterwards, for the same reason: ten options overflowed a `h-dvh overflow-hidden` page. Root `min-h-dvh`, back button in a `fixed ... top-0` strip, content `min-h-dvh ... pt-26.25` with `mt-auto`; its primary button was already in flow.
INPUT LIMITS, all enforced with `maxLength` so typing simply stops rather than being truncated on save, and all announced through the shared `WarningBanner` (the event page's 'nearly full' box, reused unchanged) the moment the cap is reached. Event TITLE 20 — the title is `text-heading-1` (35px semibold) on the event page and must not wrap, and at roughly 18px per character across 343px of content one line holds about 19, so 20 is the nearest multiple of five that fits (`Abiparty Jahrgang 26` is exactly 20). Event DESCRIPTION 500. MAX. GÄSTE 500, and this one CLAMPS rather than blocks (`Number(digits) > GUESTS_MAX ? String(GUESTS_MAX) : digits`) so the number on screen is always the number that will be stored. POOLS per event 5, OPTIONS per poll 10, pool DETAILS 300, pool QUESTION 60, pool OPTION 30 — that last one is measured: an option is one `truncate`d row in PoolCard, `text-label-1` (13px) across roughly 215px once the 24px radio, the gaps and a full three-avatar voter stack have taken their share, which is about 33 characters. VORNAME and NACHNAME are capped at 20 each (`NAME_MAX` in `features/onboarding/constants/onboarding.constants.ts`, the file that is already the single source for `AVATAR_COLORS` across onboarding and the profile), enforced identically on onboarding's Name step and on `/profile/name`, with the same `Maximal 20 Zeichen` banner — 20 is the event title's number, a full name therefore tops out at 41 characters with the space, and past that the name is only ever shown truncated (ProfileScreen's Name row is `truncate`) or wrapped (the 25px heading above it). Datum, Uhrzeit, Adresse and the background have NO character limit (the background keeps its pre-existing 10 MB cap). For the two 'add another' caps the row is REPLACED by the banner rather than left there looking tappable — 'Umfrage hinzufügen' disappears at 5, 'Option hinzufügen' at 10. Messages are deliberately one short `Maximal N …` line each (`Maximal 20 Zeichen`, `Maximal 500 Gäste`, `Maximal 5 Umfragen`, `Maximal 10 Optionen`, `Maximal 60 Zeichen pro Frage`, `Maximal 30 Zeichen pro Option`): Arthur rejected full sentences twice, and the banner only has room for about 38 characters at `text-subheading-1` before it wraps. The two poll-field banners carry `pro Frage` / `pro Option` so they cannot be confused with each other or with the Details banner below them.
THE END TIME IS OPTIONAL. The time step shows only `beginnt um…` by default; below the card, directly above the button, a switch labelled `Endzeitpunkt hinzufügen` reveals the `endet um…` row. `canContinue` is `hour && minute && (!hasEndTime || (end_hour && end_minute))`, `toggleEndTime` CLEARS `end_hour`/`end_minute` when switched back off so a discarded time cannot leak into the event, and `handleFinish` builds `ends_at` as `string | null` — without an end time the (already nullable) column simply stays null, and the past-midnight roll-forward now runs inside that branch. The toggle itself was lifted out of PoolDraftForm into `components/shared/Switch.tsx` (`label`, `checked`, `onChange`) rather than copied, so the create flow's two switches are one implementation; its track also gained the `backdrop-blur-xl` the app-wide blur rule requires on `bg-tertiary` (a no-op over an already-blurred card).
THE EMOJIS NO LONGER RESTART between steps or on the finish screen. React keeps a component instance only while the element at that position stays the same TYPE: every question returned `<CreateStepLayout>` at the root so the layout — and the `FloatingEmojis` inside it — survived a step change, but the finish screen returned a plain `<div>`, a different type, so the whole layout was torn down and a fresh spawner mounted with an empty state and everything climbing from the bottom again. `CreateEventScreen`'s branches now live in a `renderStep()` function and the component always returns the same wrapper — `<div className='relative w-full min-h-dvh bg-main'><FloatingEmojis active />{renderStep()}</div>` — so the layer sits at a tree position that never changes and only the step's content is swapped underneath it. `CreateStepLayout` and `PoolDraftForm` therefore dropped `bg-main` from their roots (an opaque background would paint straight over the shared layer) and `CreateStepLayout` lost its own `FloatingEmojis` with it; the done screen lost its private wrapper and is now just `relative z-10` like every other step. Side effect, kept deliberately: `PoolDraftForm` used to be an opaque black page with no emojis at all, so opening and closing it restarted them too — it now sits on the same continuous background as the rest of the flow.
POOLS, the rest: the question placeholder is `Bringst du was mit?`; each removable option lost the lucide `X` on its right and gained a red `bg-warning` circle with a lucide `Minus` on its LEFT, mirroring the green `Plus` on 'Option hinzufügen' (still only from the third option, two are required). The overview cards moved into `features/events/components/PoolDraftCard.tsx`, which does three things: the question value uses `rowValueClass` like the options instead of white; TAPPING the card re-opens `PoolDraftForm` on that poll (it now takes an optional `draft`, seeds all four fields from it, hands back THE SAME id so the caller replaces in place rather than appending, and its button reads `Speichern`); and SWIPING it left uncovers a delete button. That swipe is hand-rolled touch handlers — drag left, release past half to settle open, otherwise spring back — with two details that matter: the delete button sits BESIDE the card inside an `overflow-hidden` flex row rather than under it, because the card is translucent `bg-secondary` and anything behind it shows straight through; and the gesture locks to an axis on the first 8px so a vertical drag stays a page scroll, which is also what separates a tap (edit) from a swipe. The action surface is transparent with a `text-warning` lucide `Trash2` — it was a solid red block first — and stays the full 80px wide, so tapping anywhere in the revealed area deletes. Touch only: there is no mouse fallback, so a poll cannot be deleted in a desktop browser.
THE FINISH SCREEN was rebuilt from a mockup and now wears the same frame as the questions instead of centring everything: `pt-26.25` + headline + `mt-auto` on the answer block, `pb-safe-rsvp` at the foot — but no ✕, no skip and no step dots, since there is nowhere left to go back to. The link card is ONE row (`Einladungslink` label, the link, a copy button) and the two stacked buttons under it collapsed into one: 'Link kopieren' became the white `bg-sheet` circle at the right end of that row — 30px (`h-7.5 w-7.5`) with a lucide `Copy` that swaps to a lucide `Check` for the same 2s the old label did — and the remaining primary button is the EXIT, still labelled `Fertig` but now the white `bg-sheet` pill rather than the grey `bg-secondary` one, so the screen has exactly one white pill like every other step (it was briefly `okay`; Arthur put `Fertig` back). The copy circle stays FILLED white rather than becoming a bare white glyph — decided with Arthur: it matches `AddressSearchField`'s white search circle, and a bare icon would carry the same visual weight as the grey link text next to it and read as decoration instead of the row's one tappable thing. The link no longer `truncate`s: its container is masked (`[mask-image:linear-gradient(to_right,#000_65%,transparent)]`, webkit-prefixed alongside) so the text FADES OUT before the copy circle. A gradient overlay is the wrong tool here — the card is translucent `bg-secondary`, so painting the card colour back over the text only dims it by its own alpha; a mask makes the text itself transparent and therefore works on any background. Headline copy is unchanged (`Deine Party ist bereit! 🎉`; the mockup's wording was 'Deine Party ist ready!').
SKELETONS IN THE CREATE FLOW went to the only three things in it that actually wait on something. Worth stating why there are only three: the flow holds every answer in local state and writes nothing until the last step, so no question is ever gated on a fetch, and the mount-time session/profile check gates no render either (the Name step draws immediately while `userId` is still null). The final insert keeps its `Spinner` in the button — an insert plus a background upload plus every poll write has no shape to stand in for, which is exactly the split the LOADING note above draws. (1) The eight preset wallpapers on the background step are ~1.1MB together and used to pop in one at a time: each is now a local `PresetTile` component holding its own `loaded` flag, shimmering with `.skeleton` and cross-fading its image in over 500ms, the same treatment the event hero and EventCard use. It has to be a component — the flag is per-tile and state cannot live inside the parent's `.map()` — and it took the `active:scale-95` with it onto the tile's wrapper, so the whole tile presses rather than just the image. (2) The upload box does the same via a `bgPreviewLoaded` flag reset inside `handlePickBg`, since a phone photo takes a moment to decode. (3) `AddressSearchField`'s loading state was a centred `Spinner` and is now three skeleton ROWS inside the usual `cardClass` with `RowDivider`s — a suggestion's shape (40px pin circle, street line, address line) is known, so it qualifies as a skeleton by the same rule. Three rows and not the full six on purpose: a block taller than most result sets would collapse noisily every time a shorter one landed. That removed the file's only `Spinner` usage, so its import went with it.
MOTION PASS over the create flow. The HEADLINE now plays `.animate-fade-in-up` on every step, keyed on its own text (`key={headline}` in `CreateStepLayout`) — the layout itself stays mounted for the whole flow, which is what keeps the emoji layer alive, so without a key React would only swap the text and the animation would never replay. The done screen's headline carries the same class directly, since it mounts fresh. The ANSWER BLOCK BELOW IS DELIBERATELY NOT ANIMATED and must stay that way: every step's card is `bg-secondary backdrop-blur-xl` over the drifting emojis, and an ancestor animating opacity OR transform becomes their backdrop root, which would leave them flat and unblurred for the animation's full 300ms — the same trap the parties empty state and ProfileScreen's cards already hit. Only LAYOUT animations (`grid-rows-[0fr]→[1fr]`, width/height) survive a backdrop-filter, which is why `Section`, `EventDescription` and `WheelSheet` all use that idiom rather than a transform. The one honest consequence: the question animates and the answer surface does not, which reads as the surface persisting while the question changes. `WarningBanner` is unanimated for the same reason (it carries its own `backdrop-blur-xl`), and `PoolDraftForm` simply has no headline to animate.
SCROLL POSITION was the other half of 'smooth', and it was a real bug rather than a missing animation: the flow scrolls the PAGE, and the two tall steps (background, pools) are normally left scrolled down, so the next question opened halfway down itself. `goToStep()` now wraps every `setStep` — `handleNext`, `handleSelectStep` and the `handleFinish` hand-off to `done` all go through it — and jumps to the top with `window.scrollTo({ top: 0 })`. Jumped, not smooth-scrolled: the content swaps in the same frame, so an animated scroll would be gliding down a page that is already the new one. `openPoolForm()`/`closePoolForm()` do the same for the poll form, which is a page of its own reached from a list that is usually scrolled (they also absorbed the `setEditingPool`/`setShowPoolForm` pairs that were repeated at four call sites).
`WheelSheet` now has an EXIT animation. It animated open (the `shown` flag flipped a frame after mount) but not closed, because the parent renders it conditionally and its `onClose` unmounted it on the spot — so the date and both time sheets grew in smoothly and then vanished. `handleClose()` intercepts: it sets `shown` false, waits `CLOSE_MS` (300, matching both transitions) and only then calls the parent's `onClose`, with a guard so a second tap on the leaving scrim cannot queue another close and a cleanup clearing the timer on unmount. BirthdayPicker gets it for free, being the wheel's other caller.
THE GRID-ROWS IDIOM IS NOW A COMPONENT, `components/shared/Collapse.tsx` (`open`, `appear`, `className`) — it was hand-rolled in `Section`, `EventDescription` and `WheelSheet` and was about to be hand-rolled four more times. It is the reveal this app reaches for by default, because it animates HEIGHT: opacity and transform both make the animating element a backdrop root and flatten every `backdrop-blur` card inside it, and almost everything in this app is such a card. `appear` starts it collapsed and unfolds on the frame after mount (there has to be a state to animate FROM); its comment also records the gap trap — a collapsed box is still a flex item, so a parent `gap` keeps rendering around it and the spacing has to live INSIDE the children. Four new uses: (1) the `endet um…` row unfolds out of the time card when the switch is flipped; (2) `PoolDraftForm`'s whole answer column unfolds on arrival, anchored bottom by `mt-auto` so it rises — that is the create-poll page's entry animation; (3) each option row unfolds when added; (4) the address suggestions unfold while typing and fold away on blur.
THE OPTION ROWS NEEDED STABLE IDS for (3) and (4) to work, and this is the subtle part: `PoolDraftForm`'s `options` was a `string[]` keyed by POSITION, so removing row 1 of three made React reuse that row's DOM for the row moving up into its place — the row that slid up would have inherited the collapse meant for the row that left, and the card would have jumped a full row height mid-animation. State is now `{ id, value }[]` off a module-level counter (`toOptions`), keyed by id; `PoolDraft.options` is still a `string[]`, since `filled` maps back on the way out. Removal folds first and drops after (`removingId` + a `COLLAPSE_MS` timer, with a guard so a second remove cannot start mid-fold and a cleanup on unmount), and only the row whose id matches `addedId` gets `appear`, so opening a saved poll does not unfold every row it already has.
`AddressSearchField` gained a `searched` flag as part of (4). Its 'Keine Adresse gefunden' line used to be driven by `results.length === 0 && value` — which was already wrong (refocusing a picked address showed it) and became visible once the box animates, since it would UNFOLD onto that line. `searched` is set only after a search actually returns and cleared when the field empties or a suggestion is picked, so the box stays shut unless there is something to show. `handlePick` also stopped clearing `results` synchronously: emptying the list while it is folding drops the height in one frame, so the clear is now deferred by `COLLAPSE_MS`. The three states (skeleton / list / not-found) became one ternary inside the single collapsing box, and the component's root is a plain `flex flex-col` with NO gap — the 12px above the field lives on an inner `pb-3` so it folds away too.
DELETING A POOL is now two animations at once over the same 300ms: the CARD slides out to the left, and the SPACE it held folds up so the list closes the gap. `PoolDraftCard` reuses its own swipe transform for the exit — `translateX(-100%)` is the row's own width, which is wider than the container by the gap and the delete button, so the card clears the screen entirely — and `CreateEventScreen` wraps each card in a `Collapse` keyed off `removingPoolId`, dropping the row from `localPools` only after `COLLAPSE_MS`. The list needed its own gapless `flex flex-col` wrapper with the 12px on an inner `pb-3`, for the gap reason above: a collapsed card is still a flex item and the parent's `gap-3` would have left a hole where the poll used to be. NOTE the exit is a transform, which does flatten the card's `backdrop-blur` as it leaves — accepted here, and consistent, because the swipe gesture already transforms the same element and flattens it the same way. `deleting` is a PROP owned by the parent rather than state inside the card: the parent is the one that knows whether the delete was accepted, and with the card flagging itself a second tap during the 300ms window would have slid a card off screen that the guard had already rejected, leaving it stuck there.
THE SITE NOW OPENS ON A SIGN-UP/SIGN-IN OVERVIEW, not straight into the signup form. `/` still bounces a visitor with no session to `/login`, and `/login` now starts on a new `'overview'` step in `AuthScreen`: `<FloatingEmojis active />` on `bg-main` with a white sheet at the bottom edge holding the headline `Sign up oder login`, `Erstelle ein Account`, `Bei Account Anmelden` and a full-width Google button. The two text buttons only flip the local step to `signup` / `signin`, so the existing email forms are unchanged and no route was added. `?step=signup` and `?step=signin` now seed that state, which is how the invite page links straight past the overview (`?step=` previously only understood `onboarding` and `reset-password`; both still work).
That sheet is ONE component, `features/auth/components/AuthSheet.tsx`, used twice: the `/login` overview and InviteScreen's anonymous gate, whose hand-rolled copy of the same markup (and its two inline brand-mark SVGs, which moved into the component) is gone. Props are `onCreateAccount` / `onSignIn` / `next` / `description?` / `showLogo?` / `appear?` — the invite gate passes `showLogo` and its 'Um an einer Party teilnehmen zu können brauchst du einen Account.' line, the login overview passes neither, per the mockup. The mockup's Apple button is GONE — Arthur dropped Apple entirely rather than pay for the Developer Program, so the provider row is now the ONE Google button at `w-full`, the mark followed by the word `Google` (it lost its `aria-label` when it gained a visible one). The headline block is `text-center`, which also centres the invite gate's explanation under it. `appear` slides the sheet up from the bottom edge on mount (`translate-y-full` → `translate-y-0`, 300ms, the shared cubic-bezier, flipped on the frame after mount so there is a state to animate FROM) and is used only on `/login`; the sheet is opaque `bg-sheet`, so a transform here flattens no backdrop-blur.
GOOGLE IS THE ONLY OAUTH PROVIDER. `signInWithGoogle`/`signUpWithGoogle` (two identical functions) collapsed into one `signInWithGoogle(next?)` in `auth.service.ts`, called by the sheet AND by SignInForm/SignUpForm's own Google buttons, so there is one OAuth path in the app. It was briefly a provider-generic `signInWithProvider(provider: OAuthProvider, next?)` while Apple was in; that and the `OAuthProvider` type went with it, so nothing in the app mentions Apple any more. The redirect changed from `redirectTo: getOrigin()` to `${origin}/callback?next=…`, which closes the gap this file flagged earlier — verified against the live project that Supabase accepts BOTH `…/callback` and `…/callback?next=%2Fe%2Fdede5343` as redirect targets and passes the query string through untouched, so an invite link survives the round-trip.
`/callback` therefore grew a PROFILE CHECK: after `exchangeCodeForSession` it reads the user and looks for their `profiles` row, and a brand-new OAuth user (who has none) goes to `/onboarding?next=…` instead of `/parties`, which is what `app/page.tsx` was silently doing for Google before the redirect moved. Its `alert(error.message)` on the code-exchange branch was also replaced — `alert` is undefined on the server and would have thrown a 500 on any failed exchange; it now logs and redirects to `/login`. The `type=recovery` branch above it is untouched and still returns early. Verified: `/callback` → `/parties`, `/callback?next=/e/dede5343` → that invite, and both `https://evil.example` and `//evil.example` still fall back to `/parties` via `sanitizeNextPath`.
WHY THERE IS NO APPLE BUTTON, so it does not get 'restored' by mistake: Sign in with Apple on the web needs a PAID Apple Developer Program membership (~99 €/year) for the Services ID and the signing key, and its client secret is a JWT that Apple expires after at most 6 months, so it has to be regenerated on a calendar reminder or logins break silently. Arthur decided against both. `/auth/v1/settings` on the live project confirms `apple: false`, `google: true`.

THE WHOLE SIGN-UP FUNNEL IS ONE WHITE SHEET NOW, built from six mockups: Sign Up → Verifizierung → Name → Geburtstag → Profilbild, plus Login with its `Password vergessen?` line. Every one of them renders through `components/shared/SheetLayout.tsx` — the panel the invite gate introduced (`fixed inset-x-0 bottom-0 rounded-t-[2.5rem] bg-sheet px-7.5 pt-7.5 pb-10`), with the step title centred inside it and `max-h-[85dvh] overflow-y-auto` so the tall Profilbild step scrolls inside itself instead of running off the top. THE WAY BACK IS NOT ON THE SHEET: Arthur moved it out to the PAGE's top-left, where every other back button in this app lives — a `fixed left-4 mt-7.5` 45px `bg-secondary backdrop-blur-xl` circle holding a lucide `ChevronLeft` (size 24, strokeWidth 3, white), identical to `SettingsPage`'s and sitting on the dark emoji background above the sheet, not inside the white card. It is a CHEVRON, not the ✕ the mockups drew. The 85dvh cap exists for it: at 92dvh the sheet's top edge rose under the button on a tall step. `AuthScreen` and `OnboardingScreen` both dropped their old centred-form layout AND their progress dots, and both now sit on `<FloatingEmojis active />` over `bg-main`, so the emoji background is continuous from the overview to the last onboarding step.
THE MOCKUPS FOR THIS ARE POSITION ONLY — Arthur said so explicitly after a first pass copied their colours and sizes literally (a charcoal `bg-quaternary` card, `font-semibold` row labels, a `text-heading-2 font-bold` title, 55px OTP boxes). Read them for WHAT sits WHERE and take every colour, size, padding and gap from the app instead. `SheetLayout` therefore exports a LIGHT-SURFACE TWIN of the dark kit rather than anything new: `sheetCardClass` / `sheetRowClass` / `sheetRowLabelClass` / `sheetRowValueClass` / `sheetRowInputClass` / `SheetRowDivider` / `sheetButtonClass` keep the dark kit's exact geometry — 50px rows, `gap-3 px-4`, the `rounded-[25px]` card whose radius is half a row so a one-row card is a pill, the 45px `h-11.25` icon circle, the 0.75px inset hairline, the shared cubic-bezier and `active:scale-95` — and swap ONLY the colours, for the pair `BirthdayPicker`'s wheel already established on this surface: `bg-button-secondary` is a container, `text-sheet-heading` is what you read, `text-sheet-body` is what you have not filled in yet, and the hairline is `bg-sheet-heading/10` where the dark one is `bg-white/10`. Row labels are NOT semibold here either, matching the profile rule. The title is `text-heading-3 font-semibold`, the same as the overview headline right next to it in the flow and as every `SettingsPage`. The primary action is the BLACK `bg-button-primary` pill the overview already uses (a white pill on a white sheet is invisible), `h-14` like the overview's buttons, and its label is `font-semibold`, as is every button label on this surface — the overview's three (`Erstelle ein Account`, `Bei Account Anmelden`, `Google`) and every step's `weiter` / `fertig` / `speichern`. They went to `font-medium` for one pass when Arthur found semibold too heavy, and he then asked for semibold back, first on the steps and then on the overview, so the whole sheet matches the dark pages' `primaryButtonClass` again, and it fades to `bg-button-primary/40` when disabled — the app's documented pattern, not `opacity`. All of these surfaces are OPAQUE, so none carries `backdrop-blur`; the only alpha in the kit is the `active:bg-black/5` press state, which is the exact counterpart of the dark kit's `active:bg-white/10`.
✕ GOES BACK ONE STEP, never forward and never nowhere: Verifizierung → Sign Up, Geburtstag → Name, Profilbild → Geburtstag, while Sign Up, Login and Name (the first onboarding step) drop back to the `Sign up oder login` overview. Onboarding's Name step therefore pushes `/login`; a user who bails there has an account but no profiles row, and `app/page.tsx` sends them straight back to `/onboarding` next time, so nothing is stranded. Only the FIRST sheet a user sees slides up (`appear` on the overview, and `PersonalDataForm` sets it on itself since it is always onboarding's first step) — later steps swap their contents inside a panel that is already standing, so the sheet never drops and rises between questions.
THE PASSWORD RULES DID NOT CHANGE, only where they are shown: `usePasswordValidation` still demands 8 characters, a capital, a digit and a symbol, and `SignUpForm` now surfaces the FIRST unmet one in the shared `WarningBanner` under the card the moment the field is non-empty, instead of the old bare red line. `weiter` is disabled until the email is non-empty AND the password passes, so the banner and the disabled button always agree. Every step's button holds a `Spinner` while its write is in flight, and the `alert(...)` calls those forms used to make (including VerifyOtpForm's `'Wrong code! '`) are now `alertError` with a German sentence over the raw Supabase detail.
GEBURTSTAG USES THE WHEEL, not the three Selectboxes it used before: `BirthdateForm` is now one row that opens `features/profile/components/BirthdayPicker` (so onboarding and the profile's age screen are the same picker), it commits the value the wheel is about to show on OPEN — the same trick the create-event flow uses, so opening and closing without scrolling keeps what was on screen — and it opens on `today − 18 years` rather than today, so nobody scrolls two decades to reach their own year. The row reads `DD.MM.YYYY` and stores `YYYY-MM-DD`. `Selectbox` now has ONE importer left (`EditAgeScreen`); it is not dead, but it is close.
PROFILBILD IN ONBOARDING IS THE PROFILE'S PICKER, on white: the 175px circle that IS the file input with its `Pencil` badge, then the 3x3 grid of the nine `AVATAR_COLORS` showing the user's real initials (which is why `firstname`/`lastname` are now props), each with a radio underneath. Photo and colour stay mutually exclusive in both directions exactly as on `EditPictureScreen`. Three colours had to change for the light background — the empty circle is `bg-button-secondary` with a `text-sheet-body` `User` glyph, the unselected radio ring is `border-sheet-body/50` instead of `border-white/30`, and the pencil badge is the black `bg-button-primary` pill with a white `Pencil` rather than the white badge it wears on the dark screen (white on white, and grey on grey once the circle behind it is empty). There is no `Überspringen` link any more. NOTHING is selected to begin with — Arthur had the random pre-selection removed, because a colour already marked reads as a choice the user made. The circle shows the default silhouette `public/images/noProfilPicture.jpg` (grey person, white ground — the asset the pre-sheet version of this screen used) through `next/image`, which needs no `remotePatterns` since it is a local file and therefore adds no `<img>` lint warning. The initials only appear once a colour is actually tapped. `fertig` still always leads somewhere: with neither a photo nor a colour chosen it falls back to `pickRandomAvatarColor()`, because `profiles.avatar_color` is NOT NULL and every avatar in the app already falls back to initials exactly when `avatar_url` is null. `onSuccess` is async now and the form awaits it, so the spinner covers the upload AND the profiles insert that follows. Its SPACING is `EditPictureScreen`'s, measured rather than guessed: that screen's `mt-6` grid and `mt-4` button sit in `SettingsPage`'s `gap-3` column, so the real gaps are 36px avatar→grid and 28px grid→button. Reproducing them here needed the three parts wrapped in ONE flex column, otherwise the sheet's own `gap-5` would compound with the margins; the grid carries `mt-9` and the button `mt-7`. The button is also the only one in the funnel at 50px (`cn(sheetButtonClass, 'h-12.5')`, twMerge resolving the h-14 the shared class carries) — Arthur asked for that height on this screen specifically, to match the profile's save button.
THE PASSWORD RESET IS ON THE SHEET TOO now, at Arthur's request — it was the last auth state on the old dark layout. `ChangePasswordForm` was rewritten as a step like any other (`SheetLayout title='Passwort'`, the back chevron to `/login`, the two-row card `neues Passwort` / `Passwort wiederholen`, a `Spinner` in a lowercase `speichern`) and it now carries the profile screen's LIVE warnings rather than alerting on save: a mismatch outranks the strength hint because it is the one that disables the button, and both show in `WarningBanner`. Its two `alert()` calls became `alertError`. Both of its callers were restyled to match: `AuthScreen` dropped the dark early-return and renders it as `effectiveStep === 'reset-password'` alongside the other steps (that const came back for this — the URL's step has to win over local state), and `app/(auth)/forgot-password/page.tsx`, which was still on the DEAD `w-screen h-screen bg-background-main` tokens, is now the same `bg-main` page with `<FloatingEmojis active seed />` behind the sheet. Note the two entry points differ only in where they land afterwards: `/login?step=reset-password` passes an `onSuccess` to `/parties`, `/forgot-password` uses the form's own default, which is also `/parties`. NOT VERIFIED ON A DEVICE: these sheets are `position: fixed` at the bottom and now contain text inputs, so iOS Safari's keyboard behaviour over them is worth one real-phone check.

EVERY BUTTON IN THE APP IS `rounded-full`. Arthur asked for it outright, so the `rounded-2xl` on the sheet's primary pill, on the overview sheet's three buttons and on the ••• dropdown's rows (both event screens) is gone, and `primaryButtonClass` swapped its `rounded-[25px]` for `rounded-full` — that last one is a NO-OP on screen (25px was already half of its 50px height) and was changed only so the rule reads literally in the source. What is deliberately NOT rounded-full, because none of it is a button: the six OTP boxes on the Verifizierung step (inputs, and at 50px tall a full radius would draw six ovals), `Selectbox`'s trigger and the remaining `rounded-xl` text inputs (they pair with each other, not with the buttons), the `rounded-[25px]` cards, `EventMap`, `WarningBanner`, `WheelSheet`'s selection band and every skeleton. One radius must NEVER be swapped for `rounded-full`: the ••• menu's own `menuContainerClass` is a CONSTANT `rounded-[22.5px]`, exactly half its 45px closed size, because animating 9999px down to a small radius reads as a pop — that is documented above and still holds.

THE BIRTHDAY WHEEL COULD NOT BE CLOSED, and the cause is a trap worth remembering: `SheetLayout`'s panel animates with a TRANSFORM, and a transformed ancestor becomes the containing block for `position: fixed` DESCENDANTS. `BirthdateForm` rendered `BirthdayPicker` inside the sheet, so the wheel's full-screen scrim resolved against the SHEET instead of the viewport — it covered only the white card, tapping the page outside it hit nothing, and the wheel itself was clipped by the sheet's `overflow-y-auto`. The picker is now a SIBLING of `SheetLayout` (the form returns a fragment), which puts it back on the viewport: tapping anywhere outside the wheel closes it and keeps whatever was scrolled under the band, since `onChange` already commits live. The same applies to anything else fixed that ever needs to sit inside one of these sheets. The page back button also dropped from `z-50` to `z-30` as part of this — it is the only chrome above the sheet, the two never overlap thanks to the 85dvh cap, and at `z-50` it sat ABOVE the wheel's `z-40` scrim, so a tap up there navigated back a step instead of closing the wheel.

THE VERIFIZIERUNG STEP SUBMITS ITSELF. Six digits is the whole answer, so filling the last box verifies the code — Arthur asked not to have to reach for `weiter` afterwards. It is driven by the EVENT that completes the code, not by an effect watching it: `useOtpInput` gained an optional `onComplete(code)` that fires from `handleChange` and `handlePaste` the moment the array is full, and is handed the finished code because the state it comes from has not rendered yet. An effect was written first and REJECTED by this repo's React 19 lint (`react-hooks/set-state-in-effect`, the same rule `FloatingEmojis` works around with a rAF) — worth remembering, since 'watch the value, then act' is the obvious first instinct here and it does not compile. That also forced the two handlers off their functional `setDigits(prev => …)` updaters onto a plain `commit(next)` that computes the array first, so the callback is not a side effect inside a state updater. In the component `verify` is a function DECLARATION rather than a const arrow, because it and the hook are mutually dependent — the hook call needs `verify`, `verify` needs the hook's `setDigits`/`inputRefs` — and only a hoisted declaration can be referenced above its own line. On a WRONG code the boxes are emptied and the first is refocused, which readies the retry and, more importantly, is what stops the auto-submit from firing again on the same digits, so no attempt-tracking ref is needed. The `weiter` button STAYS, disabled until six digits: it is the fallback for a retry and for any browser that fills the boxes without firing `onChange`. The first box also carries `autoComplete='one-time-code'` so iOS offers the code from the SMS/mail banner.

SCROLLBARS ARE HIDDEN APP-WIDE. globals.css applies `scrollbar-width: none` / `-ms-overflow-style: none` and an empty `*::-webkit-scrollbar` to EVERYTHING, not just the page: the auth sheets, the wheel columns and the address list all scroll inside themselves, and a grey bar in any of them reads as chrome in an app that has none. Scrolling itself is untouched — only the indicator is gone. The older `.scrollbar-none` utility (still on EventDetailScreen's stats row) is now redundant but harmless and was left in place. The two rules sit at the TOP LEVEL, outside `@layer base`, and must stay there: inside the layer, Turbopack's DEV css pipeline DROPS them while `next build` keeps them, so scrollbars stayed visible in dev and vanished only in production. That was diagnosed by diffing the two emitted stylesheets — `.next/static/chunks/*.css` from a build had `*{scrollbar-width:none}`, the dev chunk had only `.scrollbar-none` — after first suspecting a stale dev server and restarting it changed nothing. General lesson for this repo: a globals.css edit must be verified against BOTH the dev chunk and a real `next build`, because the two pipelines do not agree.
A POLL QUESTION ALWAYS ENDS IN A QUESTION MARK. `PoolDraftForm`'s `withQuestionMark()` runs when the draft is saved, not while typing (which would fight the cursor on every keystroke): a trailing full stop is REPLACED, nothing is added when the text already ends in `?` or `!` — the second is an imperative prompt like `Bring was mit!`, where appending would only produce `!?` — and otherwise a `?` is appended. It runs on the trimmed value and applies to editing an existing draft as well, since both go through the same `onAdd`. One consequence worth knowing: a question typed to exactly `QUESTION_MAX` (60) is stored at 61 characters. That is deliberate — the cap is a display measurement, and truncating a character the host typed to make room for punctuation would be worse.

SIGNING UP WITH A TAKEN EMAIL IS CAUGHT ON THE SIGN UP STEP now, in the shared `WarningBanner`, reading `Diese Email hat schon ein Konto`. The detection is the subtle part and must not be 'simplified' away: Supabase deliberately will NOT say that an address exists, because that would make the signup form an account-enumeration oracle for every user in the project. With email confirmation ON — `mailer_autoconfirm` is `false` on this project, checked at `/auth/v1/settings` — a repeat signup returns SUCCESS with a DECOY user whose `identities` array is EMPTY and no session, and that empty array is the documented way to spot it (`data.user?.identities?.length === 0`; `?.` matters, since undefined must not count). The `already registered` error string is also handled, but it is only reachable if email confirmation is ever switched off. The verdict clears when the address is edited, since that is a different question. This closes the `duplicate key value violates unique constraint "profiles_pkey"` Arthur hit: without it the flow walked on to Verifizierung, whose code signed the user into the EXISTING account, and onboarding's final insert then collided with the profiles row that account already had. In the banner a taken address outranks the password-strength hint — it is the one the user cannot fix by typing more.


THE BIRTHDAY WHEEL NOW SPANS 91 YEARS, ages 10 to 100 (`MIN_AGE`/`MAX_AGE` in `BirthdayPicker.tsx`, so `YEARS` is derived rather than a hand-counted `length: 31`). It used to run `CURRENT_YEAR - 40 … - 10`, which simply shut out anyone over 40 — they could not enter their own birthday at all. ONE change covered both places Arthur named, because onboarding's `BirthdateForm` and the profile's `EditAgeScreen` already render the SAME `BirthdayPicker`; there is no second year list anywhere. `YEARS.indexOf(year)` also gained a `Math.max(0, …)`: a stored year outside the list returns -1 and would leave the wheel parked between rows, which was already possible before and is now merely unlikely.
ERRORS ARE SPLIT BETWEEN TWO SURFACES BY ONE RULE, decided with Arthur: the test is NOT whose fault it is but whether the user can do something about it ON THE SCREEN THEY ARE STANDING ON. Fixable there → the inline `WarningBanner`, next to the field they are already looking at, updating live as they type. Not fixable there → the blocking `alertError`, which is unmissable and carries the raw Supabase string worth screenshotting. That is why a wrong password is a BANNER (retyping fixes it) while a failed upload is an ALERT, even though the first is 'the user's mistake' and the second is not — blame is the wrong axis. The mapping lives in ONE file, `features/auth/services/auth-errors.ts`: `authBannerMessage(error)` looks the Supabase error CODE up in `BANNER_MESSAGES` and returns null for everything else, which is the caller's signal to alert. Ten codes are mapped — `invalid_credentials`, `email_not_confirmed`, `user_already_exists`/`email_exists`, `otp_expired`, `weak_password`, `same_password`, `over_request_rate_limit`, `over_email_send_rate_limit`, `validation_failed`. `AuthError.code` is properly typed in @supabase/auth-js 2.104.1, so this matches on codes and NOT on error message strings, which are English prose and change between releases. Every message must fit ONE LINE beside the banner's icon: about 35 characters at `text-subheading-1` in the narrowest place the banner renders, which is the auth sheet (~257px of text width after the sheet padding, the banner padding, the 18px icon and its gap). All ten are 21–32.
Wired into all five surfaces: `SignInForm` (both the login step and the Passwort-vergessen step have their own banner), `SignUpForm` (the server warning now REPLACES the old `emailTaken` boolean and outranks the live password hint), `VerifyOtpForm` (a wrong or expired code was an alert and is now a banner — mistyping is the most ordinary thing that happens on that screen), `ChangePasswordForm`, and onboarding's `ProfilePictureForm` (wrong file type and >5MB were alerts, now banners). Every one of them CLEARS its warning when the relevant input changes, so a stale verdict cannot sit under a field the user has already corrected. The alerts deliberately left behind are exactly the ones the rule assigns to them: the profile insert failing, the avatar upload failing, no session, and the generic fallback in each form for a code the mapping does not know.

EVERY EVENT WAS TWO HOURS LATE, and the cause is the classic timestamptz trap. `handleFinish` built `event_date` as a NAIVE string — `${year}-${month}-${day}T${hour}:${minute}:00`, no offset — and both `events.event_date` and `events.ends_at` are `timestamp with time zone` on a database whose session timezone is UTC, so Postgres read the picked wall-clock time AS UTC. The detail and invite screens then format it with `toLocaleTimeString('de-DE')` in the viewer's zone, which is correct, so 19:00 came back as 21:00 in CEST. Worse, the two columns DISAGREED: `ends_at` already went through `.toISOString()` and so carried the offset, while `event_date` did not — and `new Date(event_date)`, used to derive the end, parsed that naive string as LOCAL, so the end was computed from a start two hours away from the stored one. The picked time is now built as a real local `Date` from its parts and sent as `start.toISOString()`, and `ends_at` is derived from that same `start` object rather than re-parsing the string, so the past-midnight roll-forward still works and both columns agree. Verified under `TZ=Europe/Berlin`: 19:00 stores as `17:00:00.000Z` and reads back as 19:00. The DISPLAY side was always right and was not touched. One existing row (`Hausparty`) was written before the fix and was corrected in place with `timezone('Europe/Berlin', timezone('UTC', event_date))`, which shifts by that date's real offset rather than a hardcoded two hours.

---

## Behavioral guidelines
These apply on top of all project-specific rules above.
**Tradeoff:** These guidelines bias toward caution over speed. For trivial tasks, use judgment.

### If you can choose from using "" or '' just the latter
Don't ask. Don't overthink, but doublecheck if you can really use '' instead of ""

### Think before coding
Don't assume. Don't hide confusion. Surface tradeoffs.

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them — don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

### Simplicity first
Minimum code that solves the problem. Nothing speculative.

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

### Surgical changes
Touch only what you must. Clean up only your own mess.

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it — don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

The test: every changed line should trace directly to the user's request.

### Goal-driven execution
Define success criteria. Loop until verified.

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.